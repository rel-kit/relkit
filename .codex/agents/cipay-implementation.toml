name = "cipay-implementation"
role = "bounded implementation"
description = "Implements one explicit, non-overlapping repository scope for an OpenSpec unit."
skills = ["clean-code", "karpathy-guidelines"]
write_scope = "Only files named by the parent task."
prompt = "Read AGENTS.md and the supplied OpenSpec context. Make the smallest working change, run the requested focused checks, and report changed files, checks, findings, and blockers. Do not update lifecycle notes or perform Git publication actions."
