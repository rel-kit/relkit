# Blockers

- Resolved for task 9.2: the implementation-size guard reported 13 files over 200 lines; minimal behavior-preserving module splits reduced every reported file to the limit, and the final `bun run verify` passed. `bun run check`, `bun run typecheck`, `bun run test:all`, and standalone `bun run build` also pass. The direct Phase 0 sandbox child-install temp directory remains unwritable, so its recorded pass uses the `/private/tmp` Bun temp/cache workaround. Cloud acceptance and live model calls were intentionally not enabled; no authorized-cloud evidence is available. The remaining Konsistent barrel findings are advisory.
- Resolved in this follow-up: the declaration scanner's 11 false positives for structural `Effect` protocol text and private `@zsys/functions` helper handler properties.
- Resolved during task 5.1 validation: the job/event contract fixtures used a two-argument handler and treated the absent request as invocation context; both now name the unused request parameter and the contracts pass.
- No new active blockers for task 5.5. The inspector and security gates pass; the known direct read-only service-projection expectation mismatch from earlier task 4.7 dirty work remains outside this unit and was not changed.
- No new active blockers for task 6.4. The direct Phase 0 child-install temp path still requires the recorded `/private/tmp` workaround, which passed when used; all assigned focused checks pass. The known target-schema status expectation and real-port EADDRINUSE findings remain non-gating and outside this unit.
- No new active blockers for task 7.1. Focused checks and strict OpenSpec validation pass; broad build/test/verify remain assigned to task 9.x, and provider semantic validation/runtime registry construction remain explicitly deferred to task 7.2.
- No new active blockers for task 7.2. Focused checks, packed export smoke, and strict OpenSpec validation pass; isolated Phase 0 still uses the recorded `/private/tmp` Bun temp/cache workaround, and broad build/test/verify remain assigned to task 9.x. The known target-schema status expectation and real-port `EADDRINUSE` findings remain non-gating and outside this unit.
- Resolved after the task 7.3 handoff: the in-process HTTP harness now materializes an immutable `FunctionRequest` through the shared contracts adapter, the Hono runtime reuses that adapter, and the public declaration guard permits the approved logical `model` selector while still rejecting provider details. Root typecheck, declaration scanning, affected tests, compiler tests, and Phase 0 now pass.
- No new active blockers for task 7.4. The initial packed-export failure was resolved by lazy-loading the AI SDK at invocation; the focused cohort, export smoke, Phase 0, root structural/type gates, and strict OpenSpec validation pass. Broad build/test/verify remain assigned to task 9.x.
- No new active blockers for task 7.5. The repository-owned scoped cohort, root/type/declaration/boundary/Konsistent gates, packed-export smoke, Phase 0 workaround, formatting, diff, and strict OpenSpec validation all pass; broad build/test/verify remain assigned to task 9.x.
- No new active blockers for task 7.6. The AI SDK v7 offline model/tool matrix, integration/security cohort, root/type/declaration/boundary/Konsistent gates, frozen install, packed-export smoke, Phase 0 workaround, formatting, diff, and strict OpenSpec validation all pass; broad build/test/verify remain assigned to task 9.x. The legacy provider adapter/custom turn protocol remains intentionally deferred to task 7.7.
- No new active blockers for task 7.7. The focused AI SDK/provider/deployment/product-runtime cohort, root/type/type-fixture, boundary, public-declaration, Konsistent, Phase 0 workaround, formatting, diff, and strict OpenSpec gates pass. Agent-template/docs work remains assigned to task 8; broad build/test/verify gates remain assigned to task 9.x.
- No new active blockers for task 8.1. Commerce build/check/typecheck, focused acceptance flows, root/type/boundary/Konsistent/Phase 0, formatting, diff, and strict OpenSpec gates pass. Template/documentation work remains assigned to tasks 8.2–8.5; broad build/test/verify gates remain assigned to task 9.x. The recorded Phase 0 run still uses the `/private/tmp` Bun temp/cache workaround; cloud acceptance was not enabled.
- No new active blockers for task 8.2. Generated API acceptance, root structural/type-fixture/boundary/Konsistent/Phase 0 gates, authoring scan, formatting, diff, and strict OpenSpec validation pass; the minimal template remains unchanged. Agent-template/docs work remains assigned to tasks 8.3–8.5, and broad build/test/verify gates remain assigned to task 9.x.
- No new active blockers for task 8.3. Generated agent acceptance, root structural/type-fixture/boundary/Konsistent/Phase 0 gates, authoring/public-declaration scans, formatting, diff, and strict OpenSpec validation pass; broad build/test/verify gates remain assigned to task 9.x and no live model or cloud acceptance was enabled.
- No new active blockers for task 8.4. Documentation/JSDoc, root type/boundary/type-fixture/public-declaration, Konsistent, formatting, diff, and strict OpenSpec checks pass. Generated API/CLI outputs remain intentionally deferred to task 8.5; broad build/test/verify gates remain assigned to task 9.x.
- No new active blockers for task 8.5. The initial docgen example typecheck exposed the missing `DOM.Iterable` library entry and was resolved in the docs example config; regeneration and stale-output checks now pass. The packed minimal/API/agent acceptance, docs/generator checks, formatting, diff, and strict OpenSpec validation pass. Broad verification remains assigned to task 9.x; no live model or cloud acceptance was enabled.
- No new active blockers for task 9.1. All assigned focused gates pass, including isolated Phase 0 with the recorded `/private/tmp` Bun temp/cache workaround. The broad vendored `packages/tools` filter and exploratory unavailable AWS integration test were non-gating; repository-owned tools/agents checks pass, and cloud acceptance/live model calls were not enabled. Broad `check`, `typecheck`, `test:all`, `build`, and `verify` remain assigned to task 9.2; strict acceptance mapping remains task 9.3.
- No active blocker for task 9.3. Strict OpenSpec validation, the 97-scenario acceptance mapping, active-code/public-doc legacy scan, focused cohorts, and packed template smoke are complete. Evidence qualification only: raw template source tests are not standalone checkout tests because generated-project package links are absent; the packed create smoke is the runnable template gate and passes. Cloud acceptance and live model calls remain intentionally unavailable by authorization.
