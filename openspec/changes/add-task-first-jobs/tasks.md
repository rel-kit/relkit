## 1. Phase 0 — Native feasibility and API contract freeze

Read design §§0, 6–10, 16, 18 and specs/task-runtime, jobs-provider-integrations, acceptance-verification. This gate precedes public API implementation. Exact native versions and successful experiments are implementation evidence, not support claims made by this proposal.

- [x] 1.1 Record the repository/package baseline and exact SDK/server/image digests, Bun/Node/Effect versions, host architectures, persistent backend settings and deployment variants in `tests/jobs/compatibility/manifest.json`; include separate managed/self-hosted profiles for each of the three providers.
- [x] 1.2 Inspect installed Effect exports/peers against effect-mq's published versions. Select matching Effect 4.0.0-rc.115 and effect-mq 0.7.0 compatibility; do not force mismatched peers or substitute another queue.
- [x] 1.3 Create an isolated native fixture harness under `tests/jobs/compatibility` that owns namespace, workers, storage, native API observer, failure injection and cleanup; create executable `retry-sleep.ts`, `watch-lifecycle.ts` and `version-routing.ts` probes.
- [x] 1.4 Prove native acceptance, concurrent same-key deduplication, recovery of the same logical run after a lost response, and event-receipt-to-one-run routing; record retention, native ID mapping and unknown-outcome limits (F01–F02). Evidence: tests/jobs/compatibility/evidence/inngest-native.json
- [x] 1.5 Probe keyed sleep, two distinct waits, crash/replay around each wait, immutable due times and application-attempt counting before/after waits; reject mappings that multiply retries or restart waits (F05–F07). Evidence: tests/jobs/compatibility/evidence/inngest-native.json
- [x] 1.6 Probe total-attempt retry policy, exact delay/backoff/jitter/retry-after conversion, maximum-delay horizon, active-time versus acceptance-time deadlines, distributed concurrency across replicas/versions, and worker resource isolation (F12–F14).
- [x] 1.7 Probe bounded native listing with required filters and trusted scope, cursor stability, ownership metadata, historical output/schema retrieval and safe unknown statuses; a full native scan followed by Relkit filtering does not pass.
- [x] 1.8 Probe real SDK observation lifecycle, native snapshot/subscription race closure, token expiry, cursor retention, EOF verification, bounded polling and last-observer cleanup; record actual ReadableStream/iterator cleanup interfaces (F18–F23).
- [x] 1.9 Probe immutable worker build publication, old-build resume/read, service-generation routing and locator-key retention/rotation; explicitly block unsafe native hot swaps or retirement (F15, F27–F28).
- [x] 1.10 Prove account-free Inngest Docker with external persisted Redis/PostgreSQL, supported migration/registration/authentication, durable tasks, native recurring schedules and restart; probe effect-mq PostgreSQL retryable work and Trigger Docker separately.
- [x] 1.11 Probe schedule ownership/idempotent reconciliation, overlap and misfire policies, timezone/DST handling, recovery across downtime and worker-version targeting; record native unsupported combinations (F16–F17).
- [x] 1.12 Freeze the per-variant capability matrix and native policy mappings with executable evidence links and actionable diagnostics. Advance only when at least two native engines prove the common subset and required Inngest local gates are feasible; failure of a required guarantee blocks the milestone and requires an explicit change revision. Evidence: tests/jobs/compatibility/evidence/capability-matrix.json

## 2. Phase 1 — Task/job descriptors and serializable contracts

Depends on Phase 0. Read design §§0.2–0.6, 3–4, 7 and specs/task-runtime, public-authoring, workspace-foundation. New implementation files remain at most 200 lines; split internal concerns without changing the frozen package/API surface.

- [x] 2.1 Add the browser-safe `@relkit/contracts/jobs` leaf export for structural TaskRef/JobRef, protocol constants, serializable run/control/error/page/frame types and schema type parameters; move shared reference/result types out of the functions↔jobs cycle while preserving legacy type re-exports.
- [x] 2.2 Implement `packages/jobs/src/define-task.ts` and `task-types.ts`: immutable branded descriptors, required stable id/version/input/output, literal execution mode, schema inference, metadata and exact task-owned policy fields; do not add task.invoke or a function alias.
- [x] 2.3 Implement new task-target `packages/jobs/src/define-job.ts` with required literal name, optional effective durable id, task-derived schema views, service/default/schedules/admission/client metadata and no task-policy override or handler; reject function targets in TypeScript and JavaScript. Include the exclusive deprecated profile alias and diagnostic from design §0.4 without enabling legacy execution.
- [x] 2.4 Implement `packages/jobs/src/job-name.ts` as the shared runtime/literal-type grammar, complete reserved-name set and 1–64 ASCII limit; use it for job names, managed aliases and stream names without sanitizing or discovery-order suffixes.
- [x] 2.5 Implement `packages/jobs/src/duration.ts` with the exact readable unit/decimal/zero/integer rules in design §7; validate first, then use installed Effect fromInput/fromInputUnsafe/toMillis, with safe precision/range checks and no v3 decode API.
- [x] 2.6 Validate and freeze retry defaults, total maxAttempts, full/none jitter, retry-after lower bound, resources, concurrency keys/scope, maxDuration/maxElapsed, logging, observation guarantees and field limits; reject unsupported or conflicting authoring combinations before native submission.
- [x] 2.7 Extend `packages/schema/src/{schema-impl,json-schema,standard-schema,schema-metadata,builder-composites}.ts` to preserve transform/refinement provenance through nested schemas and optional/nullable/default wrappers and expose `getJsonSchema(schema, { direction })` while preserving old no-options behavior; implement faithful input/output projection diagnostics without executing default callbacks to guess metadata.
- [x] 2.8 Implement executable identity-preserving canonical inputWire validation and type compatibility, canonicalJson/assertJsonValue checks and versioned json/void envelopes; test exactly-once business transform per acceptance, canonical-only replay and rejected non-JSON/accessor/symbol/oversize payloads.
- [x] 2.9 Define mode-specific task context and exact onStart/onSuccess/onFailure hook signatures; expose declared tasks/jobs/agents/buckets/cache and existing database/auth/constants/prompts/publishes contracts, exclude sleep from retryable/hook contexts and avoid dependencies.events/functions.
- [x] 2.10 Define typed progress emit receipts and named stream schemas/items/generations, optional durable/history requirements and live defaults; reject guarantees without declared schemas and invalid stream names.
- [x] 2.11 Implement exact RunHandle, normalized status and resultAvailability unions, selected output narrowing, list queries/pages, cancellation/retry receipts and watch/content frame contracts from design §§0.6, 4, 11–12; never conflate connection error, void output and unavailable output.
- [x] 2.12 Implement exact trigger options (including operationId), server-only AbortSignal and matching task job selector; validate delayed at/delay exclusivity, pastAt policy, idempotency key/retention and finite required observer result timeout.
- [x] 2.13 Implement ScheduleDefinition cron/every exclusive union, explicit IANA cron timezone, canonical input and overlap/misfire options plus typed server-only schedule read/write receipts.
- [x] 2.14 Add `packages/app/src/tasks.ts`, new app/jobs and root exports, jobs/adapter and jobs/server entry points, package export maps and TypeScript references; verify side-effect-free imports and browser/server declarations without adding a tasks package.
- [x] 2.15 Move the old defineJob/enqueue authoring and receipt contract behind `@relkit/jobs/legacy` and `@relkit/app/jobs/legacy`; retain function-target options and instanceId/profile/status semantics for the explicit one-release compatibility path.
- [x] 2.16 Add positive/negative declaration and package-resolution fixtures for every design §18.3 case, plus `tests/types/jobs-names.test-d.ts` and `tests/unit/job-name.test.ts`; run focused package/types/unit checks with no workers or network created by imports.
- [x] 2.17 Pin the value-direction table in design §0.5 in task/client/hook types: caller versus canonical input, raw handler output versus validated result, progress/stream input versus observed output, and returned/thrown declared failures. Test transformations in both directions and reject unavailable canonical validators (RG03–RG04).
- [x] 2.18 Add table-driven boundary checks for design §7.1: zero per field, exact decimal milliseconds/bytes, invalid RFC 3339 dates/offsets, timestamp overflow, maxAttempts 1/fractional values, factor and delay ordering, and required canonical scalar keys without expression interpolation (RG05).

## 3. Phase 2 — Configuration, discovery, bindings and graph

Depends on Phase 1. Read design §§0.1–0.3, 5, 16 and specs/compiler-graph, provider-bindings, service-orchestration, domain-services, public-authoring.

- [ ] 3.1 Update `packages/app/src/{define-app-types,define-app,app-validation}.ts` for jobs/defaults.jobs and compatibility.legacyJobs (default false); normalize once to existing capability job/profile and emit source-located one-release alias/conflict diagnostics for job/defaults.job/profile.
- [ ] 3.2 Implement `packages/jobs/src/resolve-binding.ts` for exact explicit/default/implicit selection, default profile behavior, matching task selector validation and private implicit binding; test all ambiguous/missing/default cases across multiple services.
- [ ] 3.3 Add task descriptor kind/ref support through `packages/contracts/src/{id,descriptor}.ts` and compiler discovery `{ast-prefilter-utils,evaluator-snapshot,extract}.ts`; snapshot handler/hook/schema references without executing task work.
- [ ] 3.4 Implement compiler `jobs/{discover,names}.ts` to retain canonical exported binding/origin, deduplicate named/default/service/re-export references and validate global API-name uniqueness across private, exposed, implicit and cross-service jobs.
- [ ] 3.5 Extend source conventions and `konsistent.json` only as needed for task/job files in src/<domain>/tasks and jobs; allow canonical named task exports and facade-based cross-domain imports, while preserving warning-only discovery outside conventional paths.
- [ ] 3.6 Update services `{define-service,types,guards}.ts` and service identity binding to flatten task/job members with exact generics and original descriptor identity; test nested services without making domain exposure imply browser access.
- [ ] 3.7 Update compiler `normalize-domains`, `normalize-boundaries`, `normalize-reference-index`, `normalize-graph-services` and context registry for task/job dependency members and publishes; preserve existing function/event/domain boundary behavior.
- [ ] 3.8 Implement distinct durable task/job indexes and shared graph-ID mapping (`task.`/`job.` prefixes only in graph space); diagnose same-kind duplicate IDs and synthetic collisions while allowing equal task/job durable IDs and retaining legacy graph identities.
- [ ] 3.9 Extend graph `model.ts`, `graph-edges.ts`, validation and serialization with TaskNode, the executionModel-discriminated JobNode, new target/trigger/exposure edges, policies, schemas, bindings and capability metadata; preserve all existing graph kinds.
- [ ] 3.10 Extend HookNode owner/phase unions, uses-hook, compiler `normalize-graph-nodes.ts` and `normalize-graph-edge-helpers.ts` for task start/success/failure hooks with stable synthetic IDs; test that legacy function/tool transforming hooks are unchanged.
- [ ] 3.11 Extend `packages/graph/src/registration-plan.ts` with new tasks/jobs collections and keep queues/legacy schedules separate; ensure new tasks are never converted into hidden target functions or routed to the old queue materializer.
- [ ] 3.12 Implement compiler `jobs/{manifest,diagnostics}.ts` and provider requirement validation for names/defaults, native capabilities, unsafe schema projections, operation exposure, resources, scheduling and immutable version compatibility before activation.
- [ ] 3.13 Generate `.relkit/generated/jobs.manifest.json` with the exact version-1 schema in design §0.7; integrate `generate-manifest`, `generate-manifest-format`, `generated-artifacts`, runtime.manifest.ts and contract format versions into existing deterministic cohort/hash/writeIfChanged handling.
- [ ] 3.14 Compute executable build IDs from handler/dependency closure, schema/canonical encoding, adapter wrapper/runtime version and effective execution policy; keep semantic version/public name/public fingerprint separate and diagnose detectable unsafe wait/schema version reuse.
- [ ] 3.15 Generate immutable worker entries under `.relkit/build/jobs/<buildId>/<serviceGeneration>/`, retain historical routing manifests and prevent partial activation or incompatible overwrite; test last-known-good HTTP/jobs cohort behavior.
- [ ] 3.16 Add compiler golden/semantic tests including `tests/compiler/job-names.test.ts`, reordered discovery, equal durable IDs, implicit-to-explicit migration, API rename with pinned/default-derived ID, multi-service isolation and private client projections; pass focused compiler/graph/package checks.
- [ ] 3.17 Extend `normalize-utils.ts`, `normalize-compat.ts`, `normalize-schema-validation.ts`, `normalize-graph-node.ts` and evaluator snapshot/protocol handling with task-scoped schema keys, caller/canonical directions and executable contract hashes. Verify equal function/task/job durable IDs and nested transforms survive serialized discovery without schema overwrite or witness-only validation (RG02).
- [ ] 3.18 Implement the advisory replay diagnostics in design §6.3 through existing compiler AST/diagnostic infrastructure: warn on identifiable time/random-derived waits and external work before waits, provide source/remediation, and test warning-only behavior plus stable-input-derived keys. Do not add a new lint framework or claim proof of idempotency (RG13).

## 4. Phase 3 — Common invocation and native runtime SPI

Depends on Phase 2. Read design §§0.4–0.8, 6–7, 9, 13–14, 16.2 and specs/task-runtime, function-runtime, observability. Reuse invocation scope, schema and provider ownership; production behavior never falls back to the test adapter.

- [ ] 4.1 Implement `packages/jobs/src/{adapter,capabilities,runtime}.ts` with JobsAdapterRuntime protocol 1, TaskExecutionBinding and injected TaskExecutor.execute contract; validate capabilities/method availability during materialization and keep SDK imports in integrations.
- [ ] 4.2 Implement `packages/jobs/src/submission.ts` as the sole direct/context/server/generated-client admission pipeline: trusted app/environment/scope, resolved binding/build, input validation once, canonical envelope/hash, idempotency, scheduling metadata, trace and native acceptance.
- [ ] 4.3 Implement typed unknown submission/control outcomes retaining operationId and original idempotency key; normalize duplicate receipts only when the native mapping is conclusive and reject misleading fabricated RunHandles.
- [ ] 4.4 Implement `packages/jobs/src/run-id.ts` with bounded HMAC-authenticated v1 locators, constant-time verification, persisted signing key ring/service generations and immutable historical routing; test restart, rotation, invalid locator and retired-binding impact (F26–F28).
- [ ] 4.5 Implement `packages/jobs/src/authorization.ts` for trusted scope ownership and public/authorize policy before read/list/control/watch serialization or pagination; redact projections/errors and bind continuation cursors to identity, operation, filters and schema.
- [ ] 4.6 Implement `packages/jobs/src/controls.ts` for native read/list/result/cancel/retry and schedule operations with exact receipts, finite observer timeout, safe terminal/output availability handling, retained-version retry and unsupported-operation errors.
- [ ] 4.7 Implement `packages/jobs/src/server.ts` runWithJobs bootstrapping from the compiled manifest, selective binding activation, invocation-local isolation and finally cleanup; test concurrent apps without compile/Docker/global singleton side effects.
- [ ] 4.8 Wire descriptor/client methods and `packages/functions/src/{types,clients,function-descriptor-types}.ts` through cycle-free references; preserve new trigger versus legacy enqueue result compatibility and reject undeclared dependencies.
- [ ] 4.9 Extend `packages/invocation/src/dispatcher-{scope,types}.ts` and engine dependency dispatch for detached task submission, trusted context inheritance and task ancestry tracking; ensure abort before acceptance differs from observer disconnect after acceptance.
- [ ] 4.10 Add the engine task executor and runtime-effect context materialization using existing logging/time/auth/database/constants/prompts/dependency facilities; recreate context for every attempt/resume and never persist live request/session/context objects.
- [ ] 4.11 Route native park/continuation separately before errors/hooks, validate canonical input only on replay and output before terminal success; preserve declared function/event afterMs error contracts and task retry classification.
- [ ] 4.12 Implement task lifecycle hooks as bounded observational effects with separate diagnostics, repeatable replay semantics and no value transformation or retry ownership; test suspended attempts do not emit false failure/success.
- [ ] 4.13 Keep native adapters as sole retry/sleep/deadline/concurrency owners; enforce task-ancestry prohibition on runs.result even through nested function.invoke and prevent transactions/request spans spanning native waits.
- [ ] 4.14 Implement progress/stream validation and native persisted/sent/dropped receipts, generation identity and retention errors; do not add a required run store or acknowledge unpersisted data as durable.
- [ ] 4.15 Extend tracing/observability for detached producer links, run/job/task/build/service/attempt correlation, declared/resolved edges, bounded metrics and secret-safe logs; reuse original function invocation, event publication and agent checkpoint owners.
- [ ] 4.16 Add the deterministic test-only jobs adapter and fake clock under `packages/testing`, the shared conformance harness and all design §18.4 fixtures; test runtime isolation, validation, unknown acknowledgement, cancel race and single retry ownership with focused tests.
- [ ] 4.17 Wire runtimeIntegration.registrations[].create/RuntimeProviderGeneration and the jobs-adapter-runtime marker into acquired handles, then update CLI `commands/build-server.ts` and `build-server-{runtime,invocation,registration,shutdown}.ts` for new dependency sources, readiness, jobs endpoints and idempotent cleanup by the owning registry. Keep createQueue/tick/runNext legacy-only; cover generated source and packed dev/build/start with separate native workers (RG01).
- [ ] 4.18 Implement native suspension classification before the existing handler-bridge error normalization for both sync throws and async rejections. Test that the original park value reaches its SDK unchanged, no output/failure hook is emitted for park, and returned/thrown declared failures still follow existing error guards (RG04).
- [ ] 4.19 Add the verified canonical-input branch to common admission for manual retry and schedule ticks; revalidate pinned input/schema/build/scope, allocate fresh retry run budget/deadline and clear original initial delay. Test no double input/output transform and no client-selectable bypass (RG03).
- [ ] 4.20 Implement type-preserving identity tuples, stable acceptance-derived business keys and distinct retry-operation deduplication. Cover event-to-run resolution, different numeric/string keys, duplicate first-wins semantics and locator/signing-key rotation across replay (RG06).

## 5. Phase 4 — Composite Docker recipes and first durable adapter

Depends on Phases 0–3. Read design §§0.9, 8–10 and specs/local-provider-services, jobs-provider-integrations. Gate: a fresh account-free project survives accepted work, native wait and storage/worker restart.

- [ ] 5.1 Extend `packages/local-service/src/recipe.ts` and protocol/version validators with CompositeLocalServiceRecipe version 2: container/init/worker DAG, volumes/generatedSecrets/outputs/ownership; preserve authored v1 recipes through internal single-unit normalization.
- [ ] 5.2 Implement DAG validation/planning for stable unit IDs, dependency cycles/missing references, duplicate ports/volumes, health and secret leakage before Docker mutation.
- [ ] 5.3 Extend the existing local `reconciler`, `lease` and `service-state` plus Docker `docker-materializer` for project/environment/service-generation/unit ownership, private networks, host/container endpoint routing and serialized migrations; do not create a competing daemon/store.
- [ ] 5.4 Implement persistent secret generation, supported backend migrations, worker registration and readiness that verifies native acceptance/read access and a live compatible worker; roll back only newly owned failed-start resources.
- [ ] 5.5 Implement graceful drain/shutdown with retained volumes and historical workers, idempotent repeated startup, crash lease recovery and reset --dry-run/--yes/interactive confirmation; test that reset cannot delete unrelated Docker state.
- [ ] 5.6 Add `integrations/packages/inngest/package.json`, metadata, pure `src/index.ts`, runtime entry and local-recipe/deployment exports with optional SDK dependencies and pinned compatibility profile; use factory inngest and adapter ID inngest.
- [ ] 5.7 Implement Inngest native submission/receipt/run-query/control mapping in `src/runtime/{index,task-binding,runs}.ts`, using supported SDK/management APIs and immutable function/event identities; do not return the first candidate run for an event receipt.
- [ ] 5.8 Generate Inngest createFunction configuration, native serve/connect worker registration and injected TaskExecutor entry; implement native keyed sleep without nesting a sleep-capable handler in an incompatible step.
- [ ] 5.9 Map the certified native retry budget, delay/backoff, active/elapsed deadlines, cancellation, retention, distributed caps and machine limits; refuse any unproven task/service policy at preflight.
- [ ] 5.10 Implement Inngest `src/runtime/observe.ts` using native realtime SDK cleanup plus bounded authorized reconciliation/polling for source limitations; expose continuity/source and honest history/progress guarantees.
- [ ] 5.11 Implement `src/local.ts` behind the declared local-recipe export for Inngest/Redis/PostgreSQL/worker units, pinned images, explicit Redis AOF durability, durable volumes, loopback admin ports and generated local credentials.
- [ ] 5.12 Implement local immutable-worker coexistence or actionable hot-swap refusal with an explicit drain/cancel decision; preserve old accepted builds, service generations and backend state across API/compiler restart.
- [ ] 5.13 Run native Inngest fixtures for durable acceptance, same-key recovery, waits/retries/output/query/cancel, abrupt process/container termination and storage recovery (F01–F15, F26–F28); save exact environment and results with capability evidence.
- [ ] 5.14 Run existing v1 Redis/cache/bucket Docker and local lifecycle regression suites plus composite ownership/readiness/reset tests; the phase passes only when account-free durable work remains queryable after restart.

## 6. Phase 5 — Remaining provider integrations and native scheduling

Depends on Phases 0–4. Read design §§7–10, 15–16 and specs/jobs-provider-integrations, jobs-events, local-provider-services. Each adapter's deployment variants are certified separately; the three-provider milestone requires real implementations for all three.

- [ ] 6.1 Add `integrations/packages/trigger` metadata, pure trigger factory and runtime/local-recipe/deployment exports, pinned SDK dependencies and projectRef/secretKey connections; validate common service options and keep provider extensions under native.
- [ ] 6.2 Implement Trigger task/worker generation, supported native deployment/registration, trigger and receipt recovery, scoped run read/list/cancel/retry, pinned build resolution and native policy/resource mappings.
- [ ] 6.3 Implement Trigger native `runs.subscribeToRun`, metadata/progress and named streams with scoped server proxy, SDK cleanup, output validation and version-aware reconciliation; validate actual pinned subscription contract.
- [ ] 6.4 Implement and certify Trigger's account-free Docker composite and supported onboarding/migrations/secrets separately from managed workers; do not advertise Docker checkpoint/sleep capability from cloud evidence.
- [ ] 6.5 Add `integrations/packages/effect-mq` metadata, pure effectMq factory and runtime/local-recipe/deployment exports with the Phase 0 compatible Effect/SDK pin and no application Effect schema leakage.
- [ ] 6.6 Implement effect-mq Job.make, enqueue/poll/attempts/toLayer and Worker.layer mappings, native receipts/queries/controls, retry budget and worker lifetime; default execution support is retryable and durable sleep is rejected unless separately proved.
- [ ] 6.7 Implement the effect-mq PostgreSQL recipe with native DrizzleJobStore/mqJobs schema and supported migrations owned by the jobs service; certify any Redis variant separately rather than assuming PostgreSQL evidence applies.
- [ ] 6.8 Implement effect-mq supported native metadata/progress/observation or bounded native polling with cancellation and cursor capabilities declared honestly; no Relkit retry/sleep/scheduler fallback.
- [ ] 6.9 Complete Inngest connected/managed connection and worker deployment paths with separate credential, policy and capability tests; retain isolation when two configured services use the same provider.
- [ ] 6.10 Implement shared native schedule validation/read/write dispatch plus declarative reconciliation using app/environment/service/job/schedule ownership, canonical input and native idempotency; preserve operator-owned dynamic schedules and reject ownership conflicts.
- [ ] 6.11 Complete Inngest Docker native scheduling and effect-mq JobSchedules recurrence mappings; implement Trigger scheduling only for certified variants and reject unsupported cron/every/timezone/overlap/misfire policies.
- [ ] 6.12 Order schedule reconciliation after successful worker publication, target activated compatible builds for new ticks, retain already accepted versions and recover interrupted create/update/delete without duplicate schedules (F16).
- [ ] 6.13 Test explicit schedule ownership/limits, pause/resume/delete receipts, interval boundaries, UTC/IANA/DST behavior, overlap, scheduler downtime and skip/latest/all recovery against native state (F17).
- [ ] 6.14 Run each adapter's shared native fixture subset, crash/version/watch/control/resource tests and unsupported-feature diagnostics; keep missing authorized cloud evidence labeled not-tested, never passed by mocks.
- [ ] 6.15 Update the versioned capability/evidence matrix and exact three-provider catalog/recipes; gate progress on common trigger/read/cancel equivalence, account-free scheduling and all advertised native subsets, without shrinking required Inngest behavior.
- [ ] 6.16 Add native schedule regressions for transformed/static/dynamic input, native schedule/slot deduplication independent of payload admission keys, fresh identity per tick, trusted system/operator scope, declaration removal, operator pause across no-op/build/input updates and ambiguous deletion. Preserve native owned metadata and never fabricate a browser session or silently resume/delete operator state (RG03, RG06, RG09, RG11).

## 7. Phase 6 — Generated contracts and authorized endpoints

Depends on Phases 2–5. Read design §§0.5–0.7, 11–12 and specs/typed-job-client, http-runtime, compiler-graph. Reuse existing oRPC transports, client handshake and authentication.

- [ ] 7.1 Add `packages/client-generator/src/generate-job-procedures.ts` and `generate-job-registry.ts` with literal name keys, exact trigger/run/watch/stream envelopes and projection-aware schema inference; generate the same contract from graph and serialized contract input.
- [ ] 7.2 Extend generate-contract/generate-registry and shared schema projection generation to nest jobs beside existing routes/agents/realtime, exclude private/omitted operations and fingerprint name/schema/exposure/protocol changes.
- [ ] 7.3 Emit explicit procedure path arrays and name-to-durable-ID metadata; diagnose root jobs namespace collisions and keep existing exact dotted route IDs/HTTP selectors unchanged.
- [ ] 7.4 Augment the sole JobRegistry in `@relkit/client/jobs` and generate declaration fixtures for direct dot access, useRoute/useRouteMutation paths and job hook names; reject arbitrary provider/service selectors or unexposed operations in browser contracts.
- [ ] 7.5 Add jobs dispatch routes under `packages/runtime-hono/src/jobs/` for trigger/get/list/watch/stream/cancel/retry and optional certified token operation, using /rpc path arrays and the same shared runtime pipeline for HTTP/websocket.
- [ ] 7.6 Extend rpc/client-contract/client-identity/rpc-identity negotiation with jobs protocol/capabilities/public fingerprint without changing agent protocol, websocket selection or existing route contracts; reject incompatible stale jobs clients safely.
- [ ] 7.7 Implement public versus authorize grants, trusted tenant/application/environment scope, canonical input admission and ownership verification before serializing reads, native list pages or long-lived frames.
- [ ] 7.8 Implement operation/field/stream projection enforcement and safe error redaction for forged selectors, native payloads and old schemas; never expose runtime handlers, credentials, raw request sessions or unselected output.
- [ ] 7.9 Implement signed bounded list/watch cursors, grant expiry/revalidation and supported server-proxy/native-token exchange; retain the authenticated proxy as required transport and bind short-lived tokens to exact scope/run/stream permissions.
- [ ] 7.10 Apply existing transport-security/client-authorization controls for CSRF/origin, cookies, native callback signatures, frame/payload limits, abort and cleanup; validate callbacks against service generation, target/build and authenticated execution envelope.
- [ ] 7.11 Add `tests/generator/job-dot-access.test.ts`, package-resolution/browser bundle checks and transformed input/output examples; prove server-only code/native SDKs do not enter generated browser artifacts.
- [ ] 7.12 Run type/contract/security tests for private selectors, cross-tenant locators/pages/cursors, stale fingerprints, invalid signatures, projection changes, multiple runtimes and preserved route/realtime/agent behavior.
- [ ] 7.13 Implement bounded JobAccessGrant validation without passing grants into the existing boolean-only agent/channel authorization helper; reject malformed/expired grants, abort/late completion and scope changes during renewal. Test server/system/schedule scope isolation from public jobs and preserve existing authorization callers (RG09).
- [ ] 7.14 Validate job RPC envelopes separately from business schemas so HTTP/websocket admission never repeats an input/output transform; use canonical authorization input and real canonical output validation. Verify both transports against the same transformed-schema fixture (RG03).

## 8. Phase 7 — Imperative watch and React helpers

Depends on Phase 6 and native observation adapters. Read design §§0.6–0.8, 11–12, 18 and specs/typed-job-client. Implement one controller shared by all client consumers.

- [ ] 8.1 Add `packages/client/src/jobs/{types,controller,watch,reconcile}.ts` and `@relkit/client/jobs` exports with exact watchJobRun/client/name/options signature, immutable snapshots and getSnapshot/subscribe/connect/disconnect/dispose/refetch methods.
- [ ] 8.2 Implement all idle/connecting/connected/reconnecting/disconnected/completed/unauthorized/error/disposed transitions, shared concurrent connect promise, first-snapshot settlement, immediate local subscription notification and listener exception isolation; subscribe alone must not connect.
- [ ] 8.3 Implement native snapshot-plus-subscription ordering, session epochs, sequence/cursor deduplication, pending-read abort and authoritative terminal re-read on EOF/terminal signals; stale callbacks cannot mutate a changed run or identity.
- [ ] 8.4 Implement bounded native reconnect/polling with the §0.8 defaults, one retry owner, offline pause, abortable finite reads, no overlapping polls and surfaced source/continuity/reset reasons; end-of-stream never fabricates completion.
- [ ] 8.5 Implement sticky manual disconnect, permanent dispose, bounded async teardown and refcounted sharing keyed by client/app/environment/trusted identity/grant/job/run/projection/protocol; last observer releases upstream SDK resources.
- [ ] 8.6 Implement authorization expiry/revalidation, identity/session invalidation and denied-access reconnect stop. Clear old view state on run change but preserve unrelated pending submissions; clear sensitive cache/mutation/pending state on principal/session change. Observer disconnect must never cancel accepted work.
- [ ] 8.7 Implement named content start/chunk/reset/end generations separately from run/progress state, bounded frames/bytes/item limits, cursor expiry and explicit gap/overflow errors without falsely deduplicating across attempts.
- [ ] 8.8 Extend `packages/client/src/react/procedure.ts` helpers to resolve generated jobs path arrays after exact root-key lookup; preserve existing dotted route selector behavior and public route utilities.
- [ ] 8.9 Add `packages/client/src/react/job-hooks.ts` and React exports for useJobTrigger/useJobRun/useJobCancel/useJobRetry; reuse finite mutations and the shared controller, with precise registry-driven selectors/output projections and SSR-safe behavior.
- [ ] 8.10 Extend react/keys and identity-scoped caches with environment/job/run/projection/schema/jobs-protocol; preserve separate hook instances, terminal retained reads and enabled/manual connection options under Strict Mode.
- [ ] 8.11 Fix `react/finite-hooks.ts` and pending state handling so RELKIT_JOB_SUBMISSION_UNKNOWN remains recoverable even as ORPCError; retain original input/key/operationId, expose normal mutation state and avoid automatic duplicate submissions.
- [ ] 8.12 Run deterministic/model/property tests for every controller transition and F18–F24, including lost terminal frame, token expiry, old epoch race, unsubscribe during connect, throwing listener, shared feed and repeated mount/unmount.
- [ ] 8.13 Run a browser/native SDK feed and native polling acceptance path, then the 100 unique watches/1,000 shared observers/10,000 dispose-cycle envelopes; record RSS/native reads/frame bytes/CPU and require resources to return to baseline within the cleanup bound.
- [ ] 8.14 Prepare one immutable effective request before pending tracking and RPC dispatch; preserve caller operationId/key, generate a missing operationId once per mutation, and reuse the same digest/request during permitted recovery. Implement the structured recovery window/action, input-in-memory versus metadata-in-sessionStorage rule, reload digest check and bounded/storage-unavailable behavior; never automatically replay unknown writes (RG07).
- [ ] 8.15 Test per-controller leases separately from local listeners, watch-only initialization/refetch/terminal reconciliation, disconnected refetch cleanup, and connect-promise settlement when setup is interrupted. Verify operation-specific hook selectors and that disconnecting one view leaves another view connected (RG08).

## 9. Phase 8 — Inspector jobs experience

Depends on Phases 5–7. Read design §§0.10, 14, 18 and specs/development-inspector, observability. Extend the existing app and protected Inspector API.

- [ ] 9.1 Add `packages/inspector-api/src/jobs/{definitions,runs,filters,controls,schedules}.ts` and services query support using shared authorized jobs operations plus Inspector privileges; add bounded jobs-specific collection routes to existing runtime routing.
- [ ] 9.2 Replace unpaged job-run enumeration in runtime overview and `apps/inspector/lib/jobs-model.ts` with bounded native query summaries; keep legacy job display semantically separate.
- [ ] 9.3 Enhance existing /jobs navigation and clients with Definitions/Runs/Schedules/Services tabs; implement /jobs/[id], /jobs/runs/[runId] and /tasks/[id] using encoded opaque IDs and existing table/page/graph conventions.
- [ ] 9.4 Implement definitions/task detail for API name, durable IDs, implicit/default binding, schema, context dependencies, task policy, resources, lifecycle hooks, versions, native capabilities and generated worker/service health.
- [ ] 9.5 Implement required service/job/task/status/accepted-time/run-ID filters plus optional certified fields/tags/native query; translate API-name filters to stable job IDs and reject unsupported filters before querying.
- [ ] 9.6 Implement stable bounded pagination (25 default/100 max), filter-bound cursors and multi-service merge with partial/unavailable service rows and honest exact/approximate/absent counts; no fetch-all fallback or outage-as-zero.
- [ ] 9.7 Implement run detail with normalized authoritative status, selected input/result/error, attempts/waits, separate progress/content generations, cancellation request state, stale/source/gap indicators and retained-build/schema availability.
- [ ] 9.8 Implement live page reconciliation with new-runs indication and preserved user page/filter state; use the common controller for run detail and bound native read concurrency instead of subscribing to every historical row.
- [ ] 9.9 Implement capability/permission-aware cancel/retry and native schedule list/upsert/pause/resume/delete through the shared operation receipts; show unsupported, unknown and pending outcomes without promising immediate cancellation.
- [ ] 9.10 Add `tests/inspector/job-name-filters.test.ts` and API/browser tests for required native filters, malicious filters/cursors, large history pagination, cross-tenant denial, service outage (F25) and old Inspector tab regressions.
- [ ] 9.11 Verify keyboard and screen-reader navigation, accessible table/filter/control labels and status announcements, bounded rendering and cleanup on route/tab/identity changes; pass Inspector security/accessibility checks.
- [ ] 9.12 Implement aggregate consumed-row checkpoints per design §14.3; test interleaved/tied pages, unequal service contributions, unread rows after API restart, unavailable/recovered services, invalid native continuation and oversized cursors. Never advance a fetched page past unreturned rows or embed buffered run payloads in a cursor (RG10).

## 10. Phase 9 — Deployment, migration, documentation and release gates

Depends on all earlier phases. Read design §§0.10, 16–18 and Migration Plan; specs/pulumi-aws-deployment, cli-scaffolding, developer-documentation, acceptance-verification, workspace-foundation. Completion requires executable evidence and all three requested adapters' advertised subsets; publication and cloud mutations require their existing explicit authorization.

- [ ] 10.1 Extend `packages/deploy` graph planning/integration validation and `packages/deploy-pulumi` materialization/operation execution for native worker publication, isolated resource classes, secrets/network/IAM, compatible service generations and readiness before submission/schedule activation.
- [ ] 10.2 Implement each integration's staged deployment export using native SDK/CLI/provider mechanisms rather than generic HTTP containers; record code/build/runtime/schema/adapter policy identity and actual worker execution limits.
- [ ] 10.3 Exercise local rollout/rollback with new queued submissions plus old sleeping/delayed/retrying work, lost publication response and partial failure; retain compatible old builds/keys/registrations and block unsafe retirement (F15, F27).
- [ ] 10.4 Add the exact jobs CLI commands/flags in design §§0.10/16.7 through `packages/cli`, cli-command/local/help metadata and command dispatch; reuse shared operations/receipts/permissions, keep local stop and reset --yes, add scoped selection/--dry-run preview, and verify JSON/help output.
- [ ] 10.5 Extend `packages/create-relkit/src/{options,scaffold-catalog,provider-planning-definitions,add-types,add-options,add-plan,add-resolver-resources}.ts` and related parsers/transactions for task artifacts and task-target jobs in both create and add flows.
- [ ] 10.6 Implement `--jobs inngest-docker|effect-mq-docker|trigger-docker` and matching interactive choices/capability errors; preserve existing no-flag minimal/api/agent templates, cloud/deploy none defaults and unrelated provider options.
- [ ] 10.7 Add the standalone `templates/default/v1/tasks` source fixture and generated-project config/dependencies/routes/task/job/service files, client example, tests, safe .env.example and dev/check/test/build/start scripts; align retryable fixtures to queue-only providers.
- [ ] 10.8 Extend `examples/commerce` with complete idempotent fake mail/business helpers, typed-progress CSV export, native scheduled cleanup, function/route submission, agent tool submission, direct/context calls, multi-service selection, Inspector filters, React reconnect and sleep/restart fixtures; never use real email/paid APIs.
- [ ] 10.9 Implement one-release compatibility diagnostics/import migration and fixtures for legacy function-target jobs/enqueue, old public config/profile aliases, explicit gating and untouched legacy providers/events; add extraction fixtures with task-owned policy and trigger receipts.
- [ ] 10.10 Implement migration checks for pinned versus default-derived job ID renames, implicit-to-explicit binding, task version/input incompatibility, historical runs/schedules/dedupe and old client fingerprints; explain all breaking changes without silently rewriting accepted work.
- [ ] 10.11 Create jobs documentation overview/quickstart/tasks/bindings/durations-and-sleep/execution-policy/retries-and-idempotency from the complete executable fixtures and exact API defaults in design §17.2.
- [ ] 10.12 Create jobs clients/progress-and-streams/security/schedules/inspector/docker/deployment/testing/migration/troubleshooting and the three provider guides plus comparison; document per-variant evidence, unsupported capabilities, recovery limits and stop versus reset.
- [ ] 10.13 Update existing function/service/event/agent/graph/realtime/generated-client/configuration/CLI/deployment/Inspector pages, old jobs URL redirects and sidebar metadata; retain shipped agent graph/state concepts and label all legacy enqueue examples.
- [ ] 10.14 Update `apps/docs/docgen.examples.json`, `apps/docs/scripts/generate.ts` and API/reference source inputs; regenerate tasks/jobs/client/config/service/integration references, navigation/search/sitemap/LLM outputs through existing tools rather than hand-editing generated files.
- [ ] 10.15 Run docs snippet declaration checks, all local executable examples, API/default/error/limit/reference consistency, links/search/navigation and command-help tests; reject invented framework helpers, hidden state requirements, false exactly-once/resource/sleep claims and discarded run IDs.
- [ ] 10.16 Add the eight focused test:jobs scripts from design §18.8 through existing scripts/test orchestration, root scripts, test:all, verify, prepush and CI; preserve fail-fast local default, explicit Docker gates and separately authorized cloud fixtures.
- [ ] 10.17 Extend `scripts/boundary-directions.ts`, integration protocol allowlist/metadata/catalog checks, package-resolution/browser/dependency checks, fixed workspace version synchronization and release manifests for all new exports/packages; integrations import jobs/adapter, never engine.
- [ ] 10.18 Run ≥90% branch coverage and documented transition/error coverage for new validation/binding/watch modules; prove mutation checks catch missing tenant checks, abort cleanup, stale epochs, invalid policy conversion and retry-budget multiplication.
- [ ] 10.19 Complete and attach native F01–F28 results, all TJ-001–TJ-045 requirement scenarios, naming/provider-scope fixtures and load/security evidence for each applicable pinned deployment; optional unsupported cases need explicit capability reasons, not whole-suite skips.
- [ ] 10.20 Run fresh packed-package scaffold acceptance for each advertised Docker option without repository source or accounts: install, compile, startup, trigger/watch, native schedule, restart, output retrieval, Inspector filters and documented shutdown/reset; rerun existing generator/examples/Docker regressions.
- [ ] 10.21 Add the breaking changeset and one-release migration notes using the existing fixed release cohort, documenting native capability/retention/runtime limits; validate package/build/release synchronization without staging, committing or publishing.
- [ ] 10.22 Run appropriate repository typecheck/check/test:all/build/verify and guardrail tests, plus local prepush with Docker before any separately authorized push; report unavailable cloud checks honestly and resolve all required local failures.
- [ ] 10.23 Audit final implementation against this change's API freeze, all specs, design §§1–18, migration contract, phase gates and evidence. Mark the change implementation complete only when the core account-free durable/scheduling/client/Inspector/docs gate and all three adapters' advertised subsets pass; archive/release only on a later explicit request.
- [ ] 10.24 Attach the design §18.9 RG01–RG13 regression evidence alongside TJ/F evidence. Exercise the corrected React fixture's accepted runId, original request and safe unknown-recovery UI; verify deprecated profile alias versus legacy execution and keep application-wide collision tests in compiler fixtures rather than impossible isolated type checks. Keep native feasibility failures explicit (RG12–RG13).

All paths are repository-relative. This checklist and its dependencies are the source of truth: design §§1–18 incorporate the technical contract; the delta specs own TJ-001–TJ-045; design §18.5 owns F01–F28; this file owns all ten implementation phases and file responsibilities; design Migration Plan owns the upgrade contract. The source plan is provenance only and is not an implementation dependency. Splitting internal files for the 200-line limit is allowed; changing public packages/APIs or weakening a mandatory gate requires updating this change explicitly.
