# Data model showcase

Run `bun run check`, `bun test`, then `bun run dev`. The example covers SQLite CRUD, generated Zod schemas, composite unique selectors, transactions, functional overrides, and a custom model using `this.drizzle` and `this.table`.
