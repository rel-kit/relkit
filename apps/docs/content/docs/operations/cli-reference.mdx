---
title: CLI reference
description: Generated command, argument, flag, and example reference for relkit 0.2.0.
---

{/* Generated by apps/docs/scripts/generate.ts. Do not edit. */}

The command model below is shared with the Effect CLI runtime, so nested help and this page cannot drift.

## `relkit <command>`

Convention-first TypeScript application framework

| Option | Value   | Description                  |
| ------ | ------- | ---------------------------- |
| --json | boolean | Emit machine-readable output |

### Examples

`relkit` — Convention-first TypeScript application framework

### `relkit create <name>`

Create a new RELKIT application

| Argument | Value    | Description                      |
| -------- | -------- | -------------------------------- |
| name     | required | npm package and application name |

| Option                  | Value               | Description                         |
| ----------------------- | ------------------- | ----------------------------------- |
| --template              | minimal, api, agent | Starter template                    |
| --cloud                 | aws, none           | Cloud provider                      |
| --deploy                | pulumi, none        | Deployment adapter                  |
| --directory             | string              | Destination directory               |
| --install               | boolean             | Install generated project setup     |
| --no-install            | boolean             | No install generated project setup  |
| --git                   | boolean             | Git generated project setup         |
| --no-git                | boolean             | No git generated project setup      |
| --examples              | boolean             | Examples generated project setup    |
| --no-examples           | boolean             | No examples generated project setup |
| --force-empty-directory | boolean             | Allow an existing empty destination |

#### Examples

`relkit create` — Create a new RELKIT application

### `relkit dev`

Run app, inspector, OpenAPI, and Scalar

| Option           | Value                                             | Description                                     |
| ---------------- | ------------------------------------------------- | ----------------------------------------------- |
| --project-root   | string                                            | Application directory (defaults to cwd)         |
| --port           | integer                                           | Application port                                |
| --inspector-port | integer                                           | Inspector port                                  |
| --local          | on, off                                           | Start required local services                   |
| --log-level      | all, trace, debug, info, warn, error, fatal, none | Minimum terminal severity (default: info)       |
| --verbose        | boolean                                           | Show debug events and full retained diagnostics |
| --no-color       | boolean                                           | Disable terminal colors                         |

#### Examples

`relkit dev` — Run app, inspector, OpenAPI, and Scalar

### `relkit check`

Compile descriptors, infer eligible IDs, and validate the application

| Option         | Value  | Description                             |
| -------------- | ------ | --------------------------------------- |
| --project-root | string | Application directory (defaults to cwd) |

#### Examples

`relkit check` — Compile descriptors, infer eligible IDs, and validate the application

### `relkit build`

Build the checked graph, manifest, OpenAPI, and client

| Option         | Value  | Description                             |
| -------------- | ------ | --------------------------------------- |
| --project-root | string | Application directory (defaults to cwd) |

#### Examples

`relkit build` — Build the checked graph, manifest, OpenAPI, and client

### `relkit start`

Start a built application

| Option         | Value   | Description                             |
| -------------- | ------- | --------------------------------------- |
| --project-root | string  | Application directory (defaults to cwd) |
| --port         | integer | Application port                        |

#### Examples

`relkit start` — Start a built application

### `relkit graph <command>`

Inspect deterministic application graphs

#### Examples

`relkit graph` — Inspect deterministic application graphs

#### `relkit graph print [graph]`

Print a canonical graph with services and resolved source IDs

| Argument | Value    | Description     |
| -------- | -------- | --------------- |
| graph    | optional | Graph JSON path |

| Option         | Value  | Description                             |
| -------------- | ------ | --------------------------------------- |
| --project-root | string | Application directory (defaults to cwd) |

##### Examples

`relkit graph print` — Print a canonical graph with services and resolved source IDs

#### `relkit graph check [graph]`

Validate a graph and optional hash

| Argument | Value    | Description     |
| -------- | -------- | --------------- |
| graph    | optional | Graph JSON path |

| Option         | Value  | Description                             |
| -------------- | ------ | --------------------------------------- |
| --project-root | string | Application directory (defaults to cwd) |
| --hash         | string | Expected sha256 graph hash              |

##### Examples

`relkit graph check` — Validate a graph and optional hash

#### `relkit graph diff <before> <after>`

Compare graph compatibility, including inferred identity moves

| Argument | Value    | Description         |
| -------- | -------- | ------------------- |
| before   | required | Previous graph path |
| after    | required | Next graph path     |

| Option         | Value  | Description                             |
| -------------- | ------ | --------------------------------------- |
| --project-root | string | Application directory (defaults to cwd) |

##### Examples

`relkit graph diff` — Compare graph compatibility, including inferred identity moves

### `relkit env <command>`

Inspect value-free environment contracts

#### Examples

`relkit env` — Inspect value-free environment contracts

#### `relkit env check`

Validate environment values

| Option                | Value  | Description                                                             |
| --------------------- | ------ | ----------------------------------------------------------------------- |
| --project-root        | string | Application directory (defaults to cwd)                                 |
| --environment / --env | string | Provider environment, including value-free model-provider configuration |

##### Examples

`relkit env check` — Validate environment values

#### `relkit env list`

List environment value status

| Option                | Value  | Description                                                             |
| --------------------- | ------ | ----------------------------------------------------------------------- |
| --project-root        | string | Application directory (defaults to cwd)                                 |
| --environment / --env | string | Provider environment, including value-free model-provider configuration |

##### Examples

`relkit env list` — List environment value status

#### `relkit env explain <name>`

Explain one environment variable

| Argument | Value    | Description               |
| -------- | -------- | ------------------------- |
| name     | required | Environment variable name |

| Option                | Value  | Description                                                             |
| --------------------- | ------ | ----------------------------------------------------------------------- |
| --project-root        | string | Application directory (defaults to cwd)                                 |
| --environment / --env | string | Provider environment, including value-free model-provider configuration |

##### Examples

`relkit env explain` — Explain one environment variable

#### `relkit env example`

Render or write a safe .env example

| Option                | Value   | Description                                                             |
| --------------------- | ------- | ----------------------------------------------------------------------- |
| --project-root        | string  | Application directory (defaults to cwd)                                 |
| --environment / --env | string  | Provider environment, including value-free model-provider configuration |
| --path / --file       | string  | Output path                                                             |
| --write               | boolean | Write the rendered example                                              |

##### Examples

`relkit env example` — Render or write a safe .env example

### `relkit local <command>`

Manage project-scoped local services

#### Examples

`relkit local` — Manage project-scoped local services

#### `relkit local up`

Start all declared local services

| Option         | Value   | Description                             |
| -------------- | ------- | --------------------------------------- |
| --project-root | string  | Application directory (defaults to cwd) |
| --detach       | boolean | Keep services running after exit        |

##### Examples

`relkit local up` — Start all declared local services

#### `relkit local status`

Show local service and lease status

| Option         | Value  | Description                             |
| -------------- | ------ | --------------------------------------- |
| --project-root | string | Application directory (defaults to cwd) |

##### Examples

`relkit local status` — Show local service and lease status

#### `relkit local stop`

Stop project local containers and preserve volumes

| Option         | Value  | Description                             |
| -------------- | ------ | --------------------------------------- |
| --project-root | string | Application directory (defaults to cwd) |

##### Examples

`relkit local stop` — Stop project local containers and preserve volumes

#### `relkit local reset`

Remove project local containers, volumes, and state

| Option         | Value   | Description                             |
| -------------- | ------- | --------------------------------------- |
| --project-root | string  | Application directory (defaults to cwd) |
| --yes          | boolean | Confirm reset without prompting         |

##### Examples

`relkit local reset` — Remove project local containers, volumes, and state

### `relkit doctor`

Check local prerequisites and ports

| Option           | Value   | Description                             |
| ---------------- | ------- | --------------------------------------- |
| --project-root   | string  | Application directory (defaults to cwd) |
| --port           | integer | Application port                        |
| --inspector-port | integer | Inspector port                          |
| --pulumi         | boolean | Require Pulumi                          |
| --no-pulumi      | boolean | Skip Pulumi requirements                |

#### Examples

`relkit doctor` — Check local prerequisites and ports

### `relkit deploy <command>`

Manage Pulumi deployments

#### Examples

`relkit deploy` — Manage Pulumi deployments

#### `relkit deploy init`

Init the Pulumi stack

| Option                    | Value     | Description                               |
| ------------------------- | --------- | ----------------------------------------- |
| --project-root            | string    | Application directory (defaults to cwd)   |
| --stack                   | string    | Pulumi stack name (default: development)  |
| --backend                 | string    | cloud, local, or object-storage URL       |
| --config                  | key=value | Set a non-secret Pulumi value; repeatable |
| --config-secret           | key=value | Set a secret Pulumi value; repeatable     |
| --non-interactive / --yes | boolean   | Approve protected operations              |

##### Examples

`relkit deploy init` — Init the Pulumi stack

#### `relkit deploy preview`

Preview the Pulumi stack

| Option                    | Value     | Description                               |
| ------------------------- | --------- | ----------------------------------------- |
| --project-root            | string    | Application directory (defaults to cwd)   |
| --stack                   | string    | Pulumi stack name (default: development)  |
| --backend                 | string    | cloud, local, or object-storage URL       |
| --config                  | key=value | Set a non-secret Pulumi value; repeatable |
| --config-secret           | key=value | Set a secret Pulumi value; repeatable     |
| --non-interactive / --yes | boolean   | Approve protected operations              |

##### Examples

`relkit deploy preview` — Preview the Pulumi stack

#### `relkit deploy up`

Up the Pulumi stack

| Option                    | Value     | Description                               |
| ------------------------- | --------- | ----------------------------------------- |
| --project-root            | string    | Application directory (defaults to cwd)   |
| --stack                   | string    | Pulumi stack name (default: development)  |
| --backend                 | string    | cloud, local, or object-storage URL       |
| --config                  | key=value | Set a non-secret Pulumi value; repeatable |
| --config-secret           | key=value | Set a secret Pulumi value; repeatable     |
| --non-interactive / --yes | boolean   | Approve protected operations              |

##### Examples

`relkit deploy up` — Up the Pulumi stack

#### `relkit deploy refresh`

Refresh the Pulumi stack

| Option                    | Value     | Description                               |
| ------------------------- | --------- | ----------------------------------------- |
| --project-root            | string    | Application directory (defaults to cwd)   |
| --stack                   | string    | Pulumi stack name (default: development)  |
| --backend                 | string    | cloud, local, or object-storage URL       |
| --config                  | key=value | Set a non-secret Pulumi value; repeatable |
| --config-secret           | key=value | Set a secret Pulumi value; repeatable     |
| --non-interactive / --yes | boolean   | Approve protected operations              |

##### Examples

`relkit deploy refresh` — Refresh the Pulumi stack

#### `relkit deploy outputs`

Outputs the Pulumi stack

| Option                    | Value     | Description                               |
| ------------------------- | --------- | ----------------------------------------- |
| --project-root            | string    | Application directory (defaults to cwd)   |
| --stack                   | string    | Pulumi stack name (default: development)  |
| --backend                 | string    | cloud, local, or object-storage URL       |
| --config                  | key=value | Set a non-secret Pulumi value; repeatable |
| --config-secret           | key=value | Set a secret Pulumi value; repeatable     |
| --non-interactive / --yes | boolean   | Approve protected operations              |

##### Examples

`relkit deploy outputs` — Outputs the Pulumi stack

#### `relkit deploy destroy`

Destroy the Pulumi stack

| Option                    | Value     | Description                               |
| ------------------------- | --------- | ----------------------------------------- |
| --project-root            | string    | Application directory (defaults to cwd)   |
| --stack                   | string    | Pulumi stack name (default: development)  |
| --backend                 | string    | cloud, local, or object-storage URL       |
| --config                  | key=value | Set a non-secret Pulumi value; repeatable |
| --config-secret           | key=value | Set a secret Pulumi value; repeatable     |
| --non-interactive / --yes | boolean   | Approve protected operations              |

##### Examples

`relkit deploy destroy` — Destroy the Pulumi stack

### `relkit client <command>`

Generate a client from a running application

#### Examples

`relkit client` — Generate a client from a running application

#### `relkit client pull <baseUrl> --out <directory>`

Pull a versioned client contract

| Argument | Value    | Description                    |
| -------- | -------- | ------------------------------ |
| baseUrl  | required | Running RELKIT application URL |

| Option | Value  | Description      |
| ------ | ------ | ---------------- |
| --out  | string | Output directory |

##### Examples

`relkit client pull` — Pull a versioned client contract
