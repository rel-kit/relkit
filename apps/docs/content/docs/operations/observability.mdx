---
title: Observability
description: Capture correlated local evidence, redact it before storage, and export sampled telemetry without coupling handlers to a vendor.
---

RELKIT records HTTP requests and common-engine invocations with shared graph, trace, correlation, and invocation
identities. Handlers write structured records through `context.log`; functions, providers, jobs, events, tools,
and agents contribute correlated spans without importing a telemetry SDK.

## Configure one pipeline

The commerce application combines redacted local capture, bounded retention, export sampling, Sentry, and OTLP:

<include cwd lang="ts" meta='title="examples/commerce/relkit.config.ts — telemetry"'>
  ../../examples/commerce/relkit.config.ts#telemetry
</include>

Sentry and OTLP are independent integrations. Their credentials and endpoints use named binding references, so
resolved values do not enter the application graph or Inspector payloads.

## Understand capture order

```text title="Telemetry flow"
runtime event
→ canonical record
→ capture policy
→ redaction
→ bounded local persistence and live Inspector stream
→ root trace-consistent sampling
→ independent exporters
```

Local capture is independent of external sampling. With `traceRate: 0.25`, the bounded Inspector store can keep
a complete redacted local trace even when that trace is not selected for remote export. Child spans inherit the
root trace decision. Log export follows `minimumLogLevel`; errors and diagnostics are exported unsampled by
default.

`localRetention` bounds record count, age, and bytes. It is diagnostic evidence, not durable business storage.

## Read development logs

`bun run dev` prints startup URLs, compilation and reload outcomes, HTTP completions, and application output.
The default terminal level is INFO. Use `bun run dev -- --verbose` for DEBUG events and retained error details,
`--log-level warn` to select an explicit threshold, `--no-color` for plain text, or `--json` for newline-delimited
records on stderr. An explicit level overrides verbose's DEBUG selection. `NO_COLOR` and redirected output also
disable color. Terminal filtering does not reduce captured history.

Development history is stored in `.relkit/observability/observability.duckdb`. `RELKIT_OBSERVABILITY_ROOT`
changes the directory. Node.js must be available on PATH: the CLI owns a separate Node worker running the DuckDB
driver, while generated applications continue to run on Bun. History and streaming survive backend reloads.
Only one dev session can own a database at a time.

In development, exporter delivery runs independently of collector acknowledgments. Live Inspector notifications
are published only after the local database commits the batch.

The default retention is seven days and 256 MB of retained record payloads, with oldest records removed first.
Explicit `localRetention.maxAgeMs`, `maxBytes`, and `maxEntries` override those limits. `maxRecords` bounds the
in-memory cache; it does not limit database history. The data budget excludes database metadata and temporary
database/WAL overhead. Cleanup checkpoints and normal shutdown reclaim reusable space and flush pending writes.

Existing NDJSON segments are imported automatically without changing the source files. Import provenance makes
restarts safe and preserves repeated log messages; malformed lines are counted and reported. Production retains
its existing storage and export behavior.

If storage fails, application requests continue. The Logs workspace reports degraded storage and failed or
dropped records. Restart the dev session after fixing the cause; records that failed to commit are not saved.

## Redact before storage or export

`development-redacted` allows bounded content capture while replacing protected keys and known secret values.
`maxBytes` is required and prevents oversized content from being retained. Mode `off` omits protected payload
content entirely.

<Callout type="error" title="UI masking is too late">
  Authorization and cookie headers, environment secrets, provider credentials, request bodies,
  prompts, and model results must be removed or redacted before persistence, streaming, or exporter
  queues.
</Callout>

Use synthetic secrets in tests and assert that they do not appear in records, diagnostics, generated artifacts,
or exporter payloads. Redaction is a safety boundary, not permission to log credentials.

## Diagnose exporter health

Exporter fan-out is isolated. A failed Sentry or OTLP exporter cannot block application work or the other
exporter. Failures increment health counters and create redacted local-only diagnostics that are not sent back
through the failed exporter.

Start the app and follow one identifiable request:

```sh title="Terminal — relkit-orders"
bun run dev
curl "http://localhost:3000/hello?name=Telemetry" \
  -H "x-correlation-id: docs-telemetry"
```

Open the Inspector's **Requests** view, select `/hello`, and follow its trace, logs, graph hash, generation, and
activation fingerprint. If remote telemetry is silent, inspect exporter health, queue depth, failure count, and
drop count before assuming application capture failed.

AWS hosting routes redacted structured stdout to CloudWatch through the host integration. CloudWatch is host
logging, not an application exporter, so it does not belong under `telemetry.exporters`.

<include cwd>content/generated/related/operations-observability.mdx</include>
