---
title: Functions
description: Define reusable behavior once, invoke it through the common engine, and test it without a transport.
---

A function is RELKIT's reusable unit of application behavior. It declares runtime input and output schemas,
expected errors, provider-backed dependencies, optional publication access, and one handler. Routes, jobs,
event consumers, tools, agents, and direct calls all enter the same invocation engine.

## Read a complete function

<include
  cwd
  lang="ts"
  meta='title="templates/default/v1/api/src/orders/functions/create-order.function.ts"'
>
  ../../templates/default/v1/api/src/orders/functions/create-order.function.ts
</include>

The handler receives validated input. Its return value is validated before the invocation succeeds. The example
calls `priceOrder.invoke(...)`, so the nested operation keeps validation, deadlines, cancellation, tracing, and
causation. Calling another descriptor's handler directly bypasses that contract.

## Define the boundary

<Steps>
  <Step>
    ### Start with input and output

    Put a schema beside its only consumer, or in the owning domain when several descriptors share it. Keep
    transport fields such as cookies and raw headers in the route unless they are genuine domain input.

  </Step>
  <Step>
    ### Declare failures and capabilities

    List only errors callers are expected to handle. Declare only the jobs, buckets, caches, and agents the
    handler uses. Event publication is separate: list exact event IDs in `publishes`.

  </Step>
  <Step>
    ### Implement transport-independent behavior

    Use the supplied context, honor its abort signal, and return the declared output. Let unexpected defects
    cross RELKIT's safe boundary rather than converting them into unstable public errors.

  </Step>
</Steps>

`onBefore` transforms validated input before the handler. `onAfter` transforms successful output before final
output validation. Use hooks only for function policy that must apply to every invocation source; use HTTP
middleware for request-native policy.

## Test the operation directly

<include cwd lang="ts" meta='title="templates/default/v1/api/tests/unit/orders.function.test.ts"'>
  ../../templates/default/v1/api/tests/unit/orders.function.test.ts
</include>

```sh title="Terminal — relkit-orders"
bun test tests/unit/orders.function.test.ts
```

`invokeFunction` runs the descriptor through an isolated engine. A function that needs provider clients receives
explicit test fakes; tests do not infer production replacements from `RELKIT_ENV`.

<include cwd>content/generated/related/fundamentals-functions.mdx</include>
