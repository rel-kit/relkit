---
title: Redis API
description: Generated public TypeScript reference for @relkit/redis.
---

{/* Generated by apps/docs/scripts/generate.ts. Do not edit. */}

## `index.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - ["./realtime.js" (namespace export)](#realtimejs-namespace-export)
  - ["./redis.js" (namespace export)](#redisjs-namespace-export)

---

# utils

## "./realtime.js" (namespace export)

Re-exports all named exports from the "./realtime.js" module.

**Signature**

```ts
export * from "./realtime.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/index.ts#L2)

## "./redis.js" (namespace export)

Re-exports all named exports from the "./redis.js" module.

**Signature**

```ts
export * from "./redis.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/index.ts#L1)

## `index.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [localRecipe](#localrecipe)

---

# utils

## localRecipe

**Signature**

```ts
declare const localRecipe: Readonly<{
  kind: "local-service-recipe";
  protocolVersion: 1;
  integrationId: "redis";
  recipeId: "redis-docker";
  recipeVersion: 1;
  materializerId: "docker";
  image: "redis:7.4.2-alpine@sha256:02419de7eddf55aa5bcf49efb74e88fa8d931b4d77c07eff8a6b2144472b6952";
  command: ReadonlyArray<string>;
  ports: Readonly<{ redis: 6379 }>;
  volume: Readonly<{ mountPath: "/data" }>;
  health: Readonly<{
    command: ReadonlyArray<string>;
    intervalMs: 250;
    timeoutMs: 2000;
    retries: 40;
  }>;
  outputs: ({
    ports,
  }: LocalServiceRecipeOutputContext) => Readonly<{ url: `redis://127.0.0.1:${number}` }>;
}>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/index.ts#L10)

## `realtime.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [RedisAgentStateAdapter (type alias)](#redisagentstateadapter-type-alias)
  - [RedisRealtimeAdapter (type alias)](#redisrealtimeadapter-type-alias)
  - [redisAgentState](#redisagentstate)
  - [redisRealtime](#redisrealtime)

---

# utils

## RedisAgentStateAdapter (type alias)

**Signature**

```ts
type RedisAgentStateAdapter<Options> = ProviderAdapter<
  typeof agentState,
  "redis-agent-state",
  RedisConnection<Options>,
  ProviderBehavior<RedisBehavior<Options>>
>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/realtime.ts#L28)

## RedisRealtimeAdapter (type alias)

**Signature**

```ts
type RedisRealtimeAdapter<Options> = ProviderAdapter<
  typeof realtime,
  "redis-realtime",
  RedisConnection<Options>,
  ProviderBehavior<RedisBehavior<Options>>
>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/realtime.ts#L21)

## redisAgentState

**Signature**

```ts
declare const redisAgentState: <
  const Options extends RedisOptions = RedisOptions<RedisUrlReference<string> | undefined>,
>(
  options?: Options,
) => RedisAgentStateAdapter<Options>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/realtime.ts#L41)

## redisRealtime

**Signature**

```ts
declare const redisRealtime: <
  const Options extends RedisOptions = RedisOptions<RedisUrlReference<string> | undefined>,
>(
  options?: Options,
) => RedisRealtimeAdapter<Options>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/realtime.ts#L35)

## `redis.ts`

---

## Exports Grouped by Category

- [Integrations](#integrations)
  - [redis](#redis)
- [utils](#utils)
  - [RedisAdapter (type alias)](#redisadapter-type-alias)
  - [RedisBehavior (type alias)](#redisbehavior-type-alias)
  - [RedisConnection (type alias)](#redisconnection-type-alias)
  - [RedisOptions (interface)](#redisoptions-interface)
  - [RedisUrlReference (type alias)](#redisurlreference-type-alias)
  - [assertRedisOptions](#assertredisoptions)
  - [redisConnectionContract](#redisconnectioncontract)
  - [redisIntegration](#redisintegration)
  - [redisLocalRecipe](#redislocalrecipe)

---

# Integrations

## redis

Defines a Redis-compatible cache adapter. Omit `url` for a deferred local or infrastructure source.

**Example**

```ts
import { env } from "@relkit/app/config";
import { redis } from "@relkit/redis";
const cache = redis({ url: env.secret("CACHE_URL") });
```

**Signature**

```ts
declare const redis: <
  const Options extends RedisOptions = RedisOptions<RedisUrlReference<string> | undefined>,
>(
  options?: Options,
) => RedisAdapter<Options>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/redis.ts#L67)

Since v0.2.0

# utils

## RedisAdapter (type alias)

**Signature**

```ts
type RedisAdapter<Options> = ProviderAdapter<
  typeof cache,
  "redis",
  RedisConnection<Options>,
  ProviderBehavior<RedisBehavior<Options>>
>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/redis.ts#L48)

## RedisBehavior (type alias)

**Signature**

```ts
type RedisBehavior<Options> = Options extends {
  readonly connectionTimeoutMs: infer Timeout extends number;
}
  ? Readonly<{ connectionTimeoutMs: Timeout }>
  : Readonly<Record<never, never>>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/redis.ts#L42)

## RedisConnection (type alias)

**Signature**

```ts
type RedisConnection<Options> = Options extends {
  readonly url: infer Url extends RedisUrlReference;
}
  ? Readonly<{ url: Url }>
  : Readonly<Record<never, never>>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/redis.ts#L36)

## RedisOptions (interface)

**Signature**

```ts
export interface RedisOptions<
  Url extends RedisUrlReference | undefined = RedisUrlReference | undefined,
> {
  readonly url?: Url;
  readonly connectionTimeoutMs?: number;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/redis.ts#L29)

## RedisUrlReference (type alias)

**Signature**

```ts
type RedisUrlReference<Name> = BindingValueRef<Name, string, "secret-string">;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/redis.ts#L23)

## assertRedisOptions

**Signature**

```ts
declare const assertRedisOptions: (options: RedisOptions) => void;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/redis.ts#L88)

## redisConnectionContract

**Signature**

```ts
declare const redisConnectionContract: ProviderConnectionContract;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/redis.ts#L18)

## redisIntegration

**Signature**

```ts
declare const redisIntegration: IntegrationReference<"redis">;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/redis.ts#L16)

## redisLocalRecipe

**Signature**

```ts
declare const redisLocalRecipe: ProviderLocalRecipeReference;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/redis.ts#L21)

## `agent-state-provider.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [RedisAgentStateProviderOptions (interface)](#redisagentstateprovideroptions-interface)
  - [createRedisAgentStateProvider](#createredisagentstateprovider)

---

# utils

## RedisAgentStateProviderOptions (interface)

**Signature**

```ts
export interface RedisAgentStateProviderOptions {
  readonly url: string;
  readonly profile: string;
  readonly connectionTimeoutMs?: number;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/agent-state-provider.ts#L13)

## createRedisAgentStateProvider

**Signature**

```ts
declare const createRedisAgentStateProvider: (
  options: RedisAgentStateProviderOptions,
) => AgentStateProvider & { ready(): Promise<void>; close(): Promise<void> };
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/agent-state-provider.ts#L19)

## `client.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [RedisClientOptions (interface)](#redisclientoptions-interface)
  - [RedisProtocolClient (interface)](#redisprotocolclient-interface)
  - [createRedisClient](#createredisclient)

---

# utils

## RedisClientOptions (interface)

**Signature**

```ts
export interface RedisClientOptions {
  readonly url: string;
  readonly connectionTimeoutMs?: number;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/client.ts#L1)

## RedisProtocolClient (interface)

**Signature**

```ts
export interface RedisProtocolClient {
  readonly connected: boolean;
  connect(): Promise<void>;
  close(): void;
  ping(): Promise<boolean>;
  get(key: string): Promise<string | null>;
  set(key: string, value: string, ttlMs?: number): Promise<void>;
  delete(key: string): Promise<number>;
  has(key: string): Promise<boolean>;
  increment(key: string, amount?: number): Promise<number>;
  scan(
    cursor: string,
    pattern: string,
    count: number,
  ): Promise<readonly [string, readonly string[]]>;
  type(key: string): Promise<string>;
  ttl(key: string): Promise<number>;
  command(command: string, args: readonly string[]): Promise<unknown>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/client.ts#L6)

## createRedisClient

**Signature**

```ts
declare const createRedisClient: (options: RedisClientOptions) => RedisProtocolClient;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/client.ts#L26)

## `index.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - ["./agent-state-provider.js" (namespace export)](#agent-state-providerjs-namespace-export)
  - ["./client.js" (namespace export)](#clientjs-namespace-export)
  - ["./provider.js" (namespace export)](#providerjs-namespace-export)
  - ["./realtime-provider.js" (namespace export)](#realtime-providerjs-namespace-export)
  - [runtimeIntegration](#runtimeintegration)

---

# utils

## "./agent-state-provider.js" (namespace export)

Re-exports all named exports from the "./agent-state-provider.js" module.

**Signature**

```ts
export * from "./agent-state-provider.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/index.ts#L9)

## "./client.js" (namespace export)

Re-exports all named exports from the "./client.js" module.

**Signature**

```ts
export * from "./client.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/index.ts#L6)

## "./provider.js" (namespace export)

Re-exports all named exports from the "./provider.js" module.

**Signature**

```ts
export * from "./provider.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/index.ts#L7)

## "./realtime-provider.js" (namespace export)

Re-exports all named exports from the "./realtime-provider.js" module.

**Signature**

```ts
export * from "./realtime-provider.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/index.ts#L8)

## runtimeIntegration

**Signature**

```ts
declare const runtimeIntegration: Readonly<{
  kind: "runtime-integration";
  integrationId: "redis";
  registrations: ReadonlyArray<
    | {
        capability: string;
        adapterId: string;
        protocolVersion: number;
        create: ({ profile, connection, behavior }: RuntimeProviderContext) => {
          value: RedisCacheProvider;
          ready: () => Promise<void>;
          release: () => Promise<void>;
        };
      }
    | {
        capability: string;
        adapterId: string;
        protocolVersion: number;
        create: ({ profile, connection, behavior }: RuntimeProviderContext) => {
          value: RealtimeProvider & { ready(): Promise<void>; close(): Promise<void> };
          ready: () => Promise<void>;
          release: () => Promise<void>;
        };
      }
    | {
        capability: string;
        adapterId: string;
        protocolVersion: number;
        create: ({ profile, connection, behavior }: RuntimeProviderContext) => {
          value: AgentStateProvider & { ready(): Promise<void>; close(): Promise<void> };
          ready: () => Promise<void>;
          release: () => Promise<void>;
        };
      }
  >;
}>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/index.ts#L11)

## `provider.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [RedisCacheOptions (interface)](#rediscacheoptions-interface)
  - [RedisCacheProvider (interface)](#rediscacheprovider-interface)
  - [createRedisCacheProvider](#createrediscacheprovider)

---

# utils

## RedisCacheOptions (interface)

**Signature**

```ts
export interface RedisCacheOptions {
  readonly url: string;
  readonly cacheId: string;
  readonly connectionTimeoutMs?: number;
  readonly client?: RedisProtocolClient;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/provider.ts#L5)

## RedisCacheProvider (interface)

**Signature**

```ts
export interface RedisCacheProvider extends CacheProvider {
  readonly ready: () => Promise<void>;
  readonly close: () => Promise<void>;
  readonly inspector: {
    readonly scan: (request: {
      readonly search?: string;
      readonly cursor?: string;
      readonly limit: number;
      readonly signal: AbortSignal;
    }) => Promise<{ readonly items: readonly unknown[]; readonly nextCursor?: string }>;
    readonly value: (request: {
      readonly key: string;
      readonly limit: number;
      readonly signal: AbortSignal;
    }) => Promise<unknown | undefined>;
  };
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/provider.ts#L12)

## createRedisCacheProvider

**Signature**

```ts
declare const createRedisCacheProvider: (options: RedisCacheOptions) => RedisCacheProvider;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/provider.ts#L30)

## `realtime-provider.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [RedisRealtimeProviderOptions (interface)](#redisrealtimeprovideroptions-interface)
  - [createRedisRealtimeProvider](#createredisrealtimeprovider)

---

# utils

## RedisRealtimeProviderOptions (interface)

**Signature**

```ts
export interface RedisRealtimeProviderOptions {
  readonly url: string;
  readonly profile: string;
  readonly connectionTimeoutMs?: number;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/realtime-provider.ts#L9)

## createRedisRealtimeProvider

**Signature**

```ts
declare const createRedisRealtimeProvider: (
  options: RedisRealtimeProviderOptions,
) => RealtimeProvider & { ready(): Promise<void>; close(): Promise<void> };
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/realtime-provider.ts#L15)

## `state-store.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [RedisAgentStore (type alias)](#redisagentstore-type-alias)
  - [RedisRealtimeStore (type alias)](#redisrealtimestore-type-alias)
  - [RedisStateStoreOptions (interface)](#redisstatestoreoptions-interface)
  - [RedisStateWaiter (interface)](#redisstatewaiter-interface)
  - [createRedisAgentStore](#createredisagentstore)
  - [createRedisRealtimeStore](#createredisrealtimestore)

---

# utils

## RedisAgentStore (type alias)

**Signature**

```ts
type RedisAgentStore = AgentStateStore & RedisStateWaiter;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/state-store.ts#L28)

## RedisRealtimeStore (type alias)

**Signature**

```ts
type RedisRealtimeStore = RealtimeStateStore & RedisStateWaiter;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/state-store.ts#L27)

## RedisStateStoreOptions (interface)

**Signature**

```ts
export interface RedisStateStoreOptions<State> {
  readonly url: string;
  readonly key: string;
  readonly empty: () => State;
  readonly valid: (state: unknown) => state is State;
  readonly connectionTimeoutMs?: number;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/state-store.ts#L9)

## RedisStateWaiter (interface)

**Signature**

```ts
export interface RedisStateWaiter {
  waitForChange(
    recheck: () => Promise<boolean>,
    deadlineMs: number,
    signal: AbortSignal,
  ): Promise<void>;
  ready(): Promise<void>;
  close(): Promise<void>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/state-store.ts#L17)

## createRedisAgentStore

**Signature**

```ts
declare const createRedisAgentStore: (
  options: RedisStateStoreOptions<LocalAgentState>,
) => RedisAgentStore;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/state-store.ts#L45)

## createRedisRealtimeStore

**Signature**

```ts
declare const createRedisRealtimeStore: (
  options: RedisStateStoreOptions<LocalRealtimeState>,
) => RedisRealtimeStore;
```

[Source](https://github.com/rel-kit/relkit/blob/main/integrations/packages/redis/src/state-store.ts#L30)
