---
title: Realtime API
description: Generated public TypeScript reference for @relkit/realtime.
---

{/* Generated by apps/docs/scripts/generate.ts. Do not edit. */}

## `channel-validation.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [assertChannelSchema](#assertchannelschema)
  - [copyChannelClient](#copychannelclient)
  - [copyChannelEvents](#copychannelevents)
  - [copyChannelPresence](#copychannelpresence)
  - [copyChannelReplay](#copychannelreplay)
  - [isChannelSchema](#ischannelschema)
  - [isRecord](#isrecord)
  - [validateChannelValue](#validatechannelvalue)

---

# utils

## assertChannelSchema

**Signature**

```ts
declare const assertChannelSchema: (
  value: unknown,
  name: string,
) => asserts value is StandardSchemaV1;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel-validation.ts#L62)

## copyChannelClient

**Signature**

```ts
declare const copyChannelClient: (value: unknown) => ChannelClientPolicy<ChannelGuard> | undefined;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel-validation.ts#L11)

## copyChannelEvents

**Signature**

```ts
declare const copyChannelEvents: (value: unknown) => Readonly<Record<string, StandardSchemaV1>>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel-validation.ts#L42)

## copyChannelPresence

**Signature**

```ts
declare const copyChannelPresence: (
  value: unknown,
  client: ChannelClientPolicy<ChannelGuard> | undefined,
) => ChannelPresence | undefined;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel-validation.ts#L24)

## copyChannelReplay

**Signature**

```ts
declare const copyChannelReplay: (value: unknown) => ChannelReplay | undefined;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel-validation.ts#L53)

## isChannelSchema

**Signature**

```ts
declare const isChannelSchema: (value: unknown) => value is StandardSchemaV1;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel-validation.ts#L71)

## isRecord

**Signature**

```ts
declare const isRecord: (value: unknown) => value is Record<PropertyKey, unknown>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel-validation.ts#L90)

## validateChannelValue

**Signature**

```ts
declare const validateChannelValue: (
  schema: StandardSchemaV1,
  value: unknown,
  name: string,
) => Promise<unknown>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel-validation.ts#L80)

## `channel.ts`

---

## Exports Grouped by Category

- [Realtime](#realtime)
  - [defineChannel](#definechannel)
- [utils](#utils)
  - [ChannelClientPolicy (type alias)](#channelclientpolicy-type-alias)
  - [ChannelDescriptor (type alias)](#channeldescriptor-type-alias)
  - [ChannelDescriptorAny (type alias)](#channeldescriptorany-type-alias)
  - [ChannelGuard (type alias)](#channelguard-type-alias)
  - [ChannelPresence (type alias)](#channelpresence-type-alias)
  - [ChannelReplay (interface)](#channelreplay-interface)
  - [DefineChannelOptions (interface)](#definechanneloptions-interface)
  - [MemberPresenceDescriptor (interface)](#memberpresencedescriptor-interface)
  - [isChannelDescriptor](#ischanneldescriptor)

---

# Realtime

## defineChannel

Defines a typed channel for validated backend publication and observation.

**Example**

```ts
import { defineChannel } from "@relkit/realtime";
import { z } from "@relkit/schema";
const alerts = defineChannel({
  id: "alerts",
  params: z.object({}),
  events: { posted: z.string() },
  client: { public: true },
});
```

**Signature**

```ts
declare const defineChannel: <
  const Id extends string,
  const ParamsSchema extends StandardSchemaV1,
  const Events extends Readonly<Record<string, StandardSchemaV1>>,
  const Client extends ChannelClientPolicy<ChannelGuard<InferOutput<ParamsSchema>>> | undefined,
  const Presence extends ChannelPresence<InferOutput<ParamsSchema>> | undefined = undefined,
>(
  options: DefineChannelOptions<Id, ParamsSchema, Events, Client, Presence>,
) => ChannelDescriptor<Id, ParamsSchema, Events, Client, Presence>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel.ts#L120)

Since v0.4.0

# utils

## ChannelClientPolicy (type alias)

**Signature**

```ts
type ChannelClientPolicy<Guard> =
  | { readonly public: true; readonly authorize?: never }
  | { readonly authorize: Guard; readonly public?: never };
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel.ts#L30)

## ChannelDescriptor (type alias)

**Signature**

```ts
type ChannelDescriptor<Id, ParamsSchema, Events, Client, Presence> = DescriptorBase<
  "channel",
  Id
> & {
  readonly params: ParamsSchema;
  readonly events: Events;
  readonly profile?: string;
  readonly client?: Client;
  readonly replay?: ChannelReplay;
  readonly presence?: Presence;
  readonly trigger: <Name extends Extract<keyof Events, string>>(
    params: InferInput<ParamsSchema>,
    event: Name,
    payload: InferInput<Events[Name]>,
    options?: { readonly idempotencyKey?: OperationId },
  ) => Promise<TriggerReceipt>;
} & ([Presence] extends [undefined]
    ? {}
    : {
        readonly getPresence: (
          params: InferInput<ParamsSchema>,
        ) => Promise<PresenceResult<Presence>>;
      });
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel.ts#L52)

## ChannelDescriptorAny (type alias)

**Signature**

```ts
type ChannelDescriptorAny = ChannelDescriptor<
  string,
  StandardSchemaV1,
  Readonly<Record<string, StandardSchemaV1>>,
  ChannelClientPolicy<ChannelGuard> | undefined,
  ChannelPresence | undefined
>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel.ts#L85)

## ChannelGuard (type alias)

**Signature**

```ts
type ChannelGuard<Params, Context> = (params: Params, context: Context) => MaybePromise<boolean>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel.ts#L25)

## ChannelPresence (type alias)

**Signature**

```ts
type ChannelPresence<Params, Context> =
  "count" | MemberPresenceDescriptor<StandardSchemaV1, Params, Context>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel.ts#L44)

## ChannelReplay (interface)

**Signature**

```ts
export interface ChannelReplay {
  readonly retentionMs: number;
  readonly maxEvents: number;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel.ts#L47)

## DefineChannelOptions (interface)

**Signature**

```ts
export interface DefineChannelOptions<
  Id extends string,
  ParamsSchema extends StandardSchemaV1,
  Events extends Readonly<Record<string, StandardSchemaV1>>,
  Client extends ChannelClientPolicy<ChannelGuard<InferOutput<ParamsSchema>>> | undefined,
  Presence extends ChannelPresence<InferOutput<ParamsSchema>> | undefined,
> extends DescriptorMetadata {
  readonly id: Id;
  readonly params: ParamsSchema;
  readonly events: Events;
  readonly profile?: string;
  readonly client?: Client;
  readonly replay?: ChannelReplay;
  readonly presence?: Presence;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel.ts#L93)

## MemberPresenceDescriptor (interface)

**Signature**

```ts
export interface MemberPresenceDescriptor<
  MemberSchema extends StandardSchemaV1 = StandardSchemaV1,
  Params = unknown,
  Context = unknown,
> {
  readonly member: MemberSchema;
  readonly resolve: (params: Params, context: Context) => MaybePromise<InferInput<MemberSchema>>;
  readonly maxMembers: number;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel.ts#L34)

## isChannelDescriptor

**Signature**

```ts
declare const isChannelDescriptor: (value: unknown) => value is ChannelDescriptorAny;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/channel.ts#L183)

## `dispatch.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [ChannelTriggerOptions (interface)](#channeltriggeroptions-interface)
  - [RealtimeDispatcher (interface)](#realtimedispatcher-interface)
  - [currentRealtimeDispatcher](#currentrealtimedispatcher)
  - [runWithRealtimeDispatcher](#runwithrealtimedispatcher)
  - [setActiveRealtimeDispatcher](#setactiverealtimedispatcher)

---

# utils

## ChannelTriggerOptions (interface)

**Signature**

```ts
export interface ChannelTriggerOptions {
  readonly idempotencyKey?: OperationId;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/dispatch.ts#L6)

## RealtimeDispatcher (interface)

**Signature**

```ts
export interface RealtimeDispatcher {
  readonly trigger: (request: {
    readonly channel: ChannelDescriptorAny;
    readonly params: unknown;
    readonly event: string;
    readonly payload: unknown;
    readonly options?: ChannelTriggerOptions;
  }) => MaybePromise<TriggerReceipt>;
  readonly getPresence: (request: {
    readonly channel: ChannelDescriptorAny;
    readonly params: unknown;
  }) => MaybePromise<PresenceSnapshot>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/dispatch.ts#L10)

## currentRealtimeDispatcher

**Signature**

```ts
declare const currentRealtimeDispatcher: () => RealtimeDispatcher;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/dispatch.ts#L38)

## runWithRealtimeDispatcher

**Signature**

```ts
declare const runWithRealtimeDispatcher: <Value>(
  dispatcher: RealtimeDispatcher,
  action: () => Value,
) => Value;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/dispatch.ts#L31)

## setActiveRealtimeDispatcher

**Signature**

```ts
declare const setActiveRealtimeDispatcher: (dispatcher: RealtimeDispatcher | undefined) => void;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/dispatch.ts#L27)

## `index.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - ["./channel.js" (namespace export)](#channeljs-namespace-export)
  - ["./dispatch.js" (namespace export)](#dispatchjs-namespace-export)
  - ["./operation-id.js" (namespace export)](#operation-idjs-namespace-export)
  - ["./provider-dispatcher.js" (namespace export)](#provider-dispatcherjs-namespace-export)
  - ["./provider.js" (namespace export)](#providerjs-namespace-export)
  - ["./types.js" (namespace export)](#typesjs-namespace-export)

---

# utils

## "./channel.js" (namespace export)

Re-exports all named exports from the "./channel.js" module.

**Signature**

```ts
export * from "./channel.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/index.ts#L1)

## "./dispatch.js" (namespace export)

Re-exports all named exports from the "./dispatch.js" module.

**Signature**

```ts
export * from "./dispatch.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/index.ts#L4)

## "./operation-id.js" (namespace export)

Re-exports all named exports from the "./operation-id.js" module.

**Signature**

```ts
export * from "./operation-id.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/index.ts#L3)

## "./provider-dispatcher.js" (namespace export)

Re-exports all named exports from the "./provider-dispatcher.js" module.

**Signature**

```ts
export * from "./provider-dispatcher.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/index.ts#L6)

## "./provider.js" (namespace export)

Re-exports all named exports from the "./provider.js" module.

**Signature**

```ts
export * from "./provider.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/index.ts#L2)

## "./types.js" (namespace export)

Re-exports all named exports from the "./types.js" module.

**Signature**

```ts
export * from "./types.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/index.ts#L5)

## `operation-id.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [OperationIdError (class)](#operationiderror-class)
  - [createOperationId](#createoperationid)
  - [operationIdTimestamp](#operationidtimestamp)
  - [parseOperationId](#parseoperationid)

---

# utils

## OperationIdError (class)

**Signature**

```ts
declare class OperationIdError { constructor(
    readonly code: "OPERATION_ID_INVALID" | "IDEMPOTENCY_WINDOW_EXPIRED",
    message: string,
  ) }
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/operation-id.ts#L5)

## createOperationId

**Signature**

```ts
declare const createOperationId: (now?: number) => OperationId;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/operation-id.ts#L40)

## operationIdTimestamp

**Signature**

```ts
declare const operationIdTimestamp: (value: string) => number;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/operation-id.ts#L36)

## parseOperationId

**Signature**

```ts
declare const parseOperationId: (
  value: string,
  options?: { readonly now?: number; readonly receiptWindowMs?: number },
) => OperationId;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/operation-id.ts#L15)

## `provider-dispatcher.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [ProviderRealtimeDispatcherOptions (interface)](#providerrealtimedispatcheroptions-interface)
  - [createProviderRealtimeDispatcher](#createproviderrealtimedispatcher)

---

# utils

## ProviderRealtimeDispatcherOptions (interface)

**Signature**

```ts
export interface ProviderRealtimeDispatcherOptions {
  readonly applicationId: string;
  readonly environment: string;
  readonly generationId: string;
  readonly publicFingerprint: string;
  readonly provider: (profile: string) => Promise<RealtimeProvider> | RealtimeProvider;
  readonly limits?: Partial<RealtimeLimits>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider-dispatcher.ts#L7)

## createProviderRealtimeDispatcher

**Signature**

```ts
declare const createProviderRealtimeDispatcher: (
  options: ProviderRealtimeDispatcherOptions,
) => RealtimeDispatcher;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider-dispatcher.ts#L16)

## `provider.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [AcquirePresenceLease (type alias)](#acquirepresencelease-type-alias)
  - [AppendChannelEvent (interface)](#appendchannelevent-interface)
  - [AppendReceiptLookup (type alias)](#appendreceiptlookup-type-alias)
  - [ChannelEventPage (interface)](#channeleventpage-interface)
  - [LookupAppendReceipt (interface)](#lookupappendreceipt-interface)
  - [PresenceLeaseRequest (interface)](#presenceleaserequest-interface)
  - [ReadChannelEvents (interface)](#readchannelevents-interface)
  - [ReadPresenceRequest (type alias)](#readpresencerequest-type-alias)
  - [RealtimeLimits (interface)](#realtimelimits-interface)
  - [RealtimeProvider (interface)](#realtimeprovider-interface)
  - [ReleasePresenceLease (type alias)](#releasepresencelease-type-alias)
  - [RenewPresenceLease (type alias)](#renewpresencelease-type-alias)
  - [WaitForChannelEvents (interface)](#waitforchannelevents-interface)

---

# utils

## AcquirePresenceLease (type alias)

**Signature**

```ts
type AcquirePresenceLease = PresenceLeaseRequest;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L72)

## AppendChannelEvent (interface)

**Signature**

```ts
export interface AppendChannelEvent extends RealtimeScope {
  readonly operationId?: OperationId;
  readonly semanticDigest?: string;
  readonly event: string;
  readonly payload: unknown;
  readonly encodedBytes: number;
  readonly occurredAt: string;
  readonly receiptExpiresAt?: string;
  readonly retentionMs?: number;
  readonly maxEvents?: number;
  readonly limits: RealtimeLimits;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L18)

## AppendReceiptLookup (type alias)

**Signature**

```ts
type AppendReceiptLookup = ReceiptLookup<TriggerReceipt>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L38)

## ChannelEventPage (interface)

**Signature**

```ts
export interface ChannelEventPage {
  readonly events: readonly ChannelEventRecord[];
  readonly checkpoint: ChannelCheckpoint;
  readonly hasMore: boolean;
  readonly gap?: "expired" | "foreign" | "future" | "policy" | "session" | "provider-reset";
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L46)

## LookupAppendReceipt (interface)

**Signature**

```ts
export interface LookupAppendReceipt extends RealtimeScope {
  readonly operationId: OperationId;
  readonly semanticDigest: string;
  readonly receiptWindowMs: number;
  readonly now: string;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L31)

## PresenceLeaseRequest (interface)

**Signature**

```ts
export interface PresenceLeaseRequest extends RealtimeScope {
  readonly leaseId: string;
  readonly connectionId: string;
  readonly opaqueMemberId?: string;
  readonly memberInfo?: unknown;
  readonly expiresAt: string;
  readonly maxMembers: number;
  readonly maxConnections: number;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L59)

## ReadChannelEvents (interface)

**Signature**

```ts
export interface ReadChannelEvents extends RealtimeScope {
  readonly after?: ChannelCheckpoint;
  readonly limit: number;
  readonly maxEncodedBytes: number;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L40)

## ReadPresenceRequest (type alias)

**Signature**

```ts
type ReadPresenceRequest = RealtimeScope & {
  readonly maxMembers: number;
};
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L69)

## RealtimeLimits (interface)

**Signature**

```ts
export interface RealtimeLimits {
  readonly maxEventBytes: number;
  readonly maxRetainedBytes: number;
  readonly maxPartitions: number;
  readonly maxConnections: number;
  readonly maxPresenceMembers: number;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L10)

## RealtimeProvider (interface)

**Signature**

```ts
export interface RealtimeProvider {
  readonly capabilities: {
    readonly replay: "memory" | "retained";
    readonly presenceScope: "process" | "shared";
    readonly durability: "volatile" | "filesystem" | "redis-configured";
  };
  getEpoch(): Promise<string>;
  append(request: AppendChannelEvent): Promise<TriggerReceipt>;
  lookupAppendReceipt(request: LookupAppendReceipt): Promise<AppendReceiptLookup>;
  readAfter(request: ReadChannelEvents): Promise<ChannelEventPage>;
  waitAfter(request: WaitForChannelEvents): Promise<void>;
  readPresence(request: ReadPresenceRequest): Promise<PresenceSnapshot>;
  acquirePresence(request: AcquirePresenceLease): Promise<PresenceSnapshot>;
  renewPresence(request: RenewPresenceLease): Promise<PresenceSnapshot>;
  releasePresence(request: ReleasePresenceLease): Promise<PresenceSnapshot>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L76)

## ReleasePresenceLease (type alias)

**Signature**

```ts
type ReleasePresenceLease = Omit<PresenceLeaseRequest, "expiresAt">;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L74)

## RenewPresenceLease (type alias)

**Signature**

```ts
type RenewPresenceLease = PresenceLeaseRequest;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L73)

## WaitForChannelEvents (interface)

**Signature**

```ts
export interface WaitForChannelEvents extends RealtimeScope {
  readonly after: ChannelCheckpoint;
  readonly deadlineMs: number;
  readonly signal: AbortSignal;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/provider.ts#L53)

## `types.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [ChannelCheckpoint (interface)](#channelcheckpoint-interface)
  - [ChannelEventRecord (interface)](#channeleventrecord-interface)
  - [ChannelGrant (interface)](#channelgrant-interface)
  - [CountPresence (interface)](#countpresence-interface)
  - [MemberPresence (interface)](#memberpresence-interface)
  - [PresenceMember (interface)](#presencemember-interface)
  - [PresenceSnapshot (type alias)](#presencesnapshot-type-alias)
  - [RealtimeScope (interface)](#realtimescope-interface)
  - [TriggerReceipt (interface)](#triggerreceipt-interface)

---

# utils

## ChannelCheckpoint (interface)

**Signature**

```ts
export interface ChannelCheckpoint extends RealtimeScope {
  readonly sequence: string;
  readonly historyStart: string;
  readonly expiresAt: string;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/types.ts#L14)

## ChannelEventRecord (interface)

**Signature**

```ts
export interface ChannelEventRecord {
  readonly eventId: string;
  readonly event: string;
  readonly payload: unknown;
  readonly checkpoint: ChannelCheckpoint;
  readonly encodedBytes: number;
  readonly createdAt: string;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/types.ts#L27)

## ChannelGrant (interface)

**Signature**

```ts
export interface ChannelGrant extends RealtimeScope {
  readonly grantId: string;
  readonly principalScope: string;
  readonly historyStart: string;
  readonly expiresAt: string;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/types.ts#L20)

## CountPresence (interface)

**Signature**

```ts
export interface CountPresence {
  readonly connections: number | undefined;
  readonly status: "loading" | "fresh" | "stale" | "error";
  readonly revision: string | undefined;
  readonly scope: "process" | "shared";
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/types.ts#L46)

## MemberPresence (interface)

**Signature**

```ts
export interface MemberPresence<Member = unknown> extends CountPresence {
  readonly memberCount: number | undefined;
  readonly members: readonly PresenceMember<Member>[];
  readonly membersTruncated: boolean;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/types.ts#L58)

## PresenceMember (interface)

**Signature**

```ts
export interface PresenceMember<Member = unknown> {
  readonly id: string;
  readonly info: Member;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/types.ts#L53)

## PresenceSnapshot (type alias)

**Signature**

```ts
type PresenceSnapshot<Member> = CountPresence | MemberPresence<Member>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/types.ts#L64)

## RealtimeScope (interface)

**Signature**

```ts
export interface RealtimeScope extends ExpectedClientIdentity {
  readonly applicationId: string;
  readonly environment: string;
  readonly tenantScope?: string;
  readonly channelId: string;
  readonly partition: string;
  readonly profile: string;
  readonly policyEpoch: string;
  readonly providerEpoch: string;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/types.ts#L3)

## TriggerReceipt (interface)

**Signature**

```ts
export interface TriggerReceipt {
  readonly accepted: true;
  readonly operationId?: OperationId;
  readonly eventId: string;
  readonly checkpoint: ChannelCheckpoint;
  readonly profile: string;
  readonly providerEpoch: string;
  readonly duplicate: boolean;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/realtime/src/types.ts#L36)
