---
title: Drizzle API
description: Generated public TypeScript reference for @relkit/drizzle.
---

{/* Generated by apps/docs/scripts/generate.ts. Do not edit. */}

## `activation.ts`

---

## Exports Grouped by Category

- [Database](#database)
  - [activateDrizzleService](#activatedrizzleservice)
- [utils](#utils)
  - [DrizzleActivation (interface)](#drizzleactivation-interface)

---

# Database

## activateDrizzleService

Activates a service once per descriptor object and returns its client, context, and close hook.
Repeated calls share the first activation; close does not evict the cached activation.

**Example**

```ts
import { activateDrizzleService, defineDrizzleService } from "@relkit/drizzle";
import { integer, sqliteTable } from "drizzle-orm/sqlite-core";

if (typeof Bun !== "undefined") {
  const { Database } = await import("bun:sqlite");
  const { drizzle } = await import("drizzle-orm/bun-sqlite");
  const users = sqliteTable("users", { id: integer().primaryKey() });
  const service = defineDrizzleService({
    schema: { users },
    client: () => drizzle({ client: new Database(":memory:") }),
    dispose: (database) => database.$client.close(),
  });
  const active = await activateDrizzleService(service, {});
  try {
    console.assert(active.context.users !== undefined);
  } finally {
    await active.close();
  }
}
```

**Signature**

```ts
declare const activateDrizzleService: <
  Service extends DrizzleServiceDescriptor<any, any, any, any>,
>(
  service: Service,
  env: Readonly<Record<string, unknown>>,
) => Promise<DrizzleActivation<Service>>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/activation.ts#L41)

Since v0.1.0

# utils

## DrizzleActivation (interface)

**Signature**

```ts
export interface DrizzleActivation<Service extends DrizzleServiceDescriptor<any, any, any, any>> {
  readonly client: Service extends DrizzleServiceDescriptor<any, infer Client, any, any>
    ? Client
    : never;
  readonly context: DatabaseContext<Service>;
  readonly close: () => Promise<void>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/activation.ts#L5)

## `context.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [createDatabaseContext](#createdatabasecontext)

---

# utils

## createDatabaseContext

**Signature**

```ts
declare const createDatabaseContext: <Service extends DrizzleServiceDescriptor<any, any, any, any>>(
  service: Service,
  runtime: DrizzleServiceRuntime,
  database: unknown,
) => DatabaseContext<Service>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/context.ts#L6)

## `index.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [ApplicationEnv](#applicationenv)
  - [BaseOperations](#baseoperations)
  - [DatabaseContext](#databasecontext)
  - [DeleteArgs](#deleteargs)
  - [DialectDatabase](#dialectdatabase)
  - [DrizzleCapability](#drizzlecapability)
  - [DrizzleOverrides](#drizzleoverrides)
  - [DrizzleServiceDescriptor](#drizzleservicedescriptor)
  - [FindManyArgs](#findmanyargs)
  - [FindOneArgs](#findoneargs)
  - [InsertArgs](#insertargs)
  - [ModelDescriptor](#modeldescriptor)
  - [ModelExtensionContext](#modelextensioncontext)
  - [Row](#row)
  - [TableOverrides](#tableoverrides)
  - [TableZodSchemas](#tablezodschemas)
  - [Update](#update)
  - [UpdateArgs](#updateargs)
  - [UpsertArgs](#upsertargs)
  - [Where](#where)
  - [activateDrizzleService](#activatedrizzleservice)
  - [defineDrizzleService](#definedrizzleservice)
  - [defineModel](#definemodel)

---

# utils

## ApplicationEnv

**Signature**

```ts
declare const ApplicationEnv: Readonly<Record<string, string>>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L5)

## BaseOperations

**Signature**

```ts
declare const BaseOperations: BaseOperations<T>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L6)

## DatabaseContext

**Signature**

```ts
declare const DatabaseContext: DatabaseContext<Service>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L7)

## DeleteArgs

**Signature**

```ts
declare const DeleteArgs: DeleteArgs<T>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L8)

## DialectDatabase

**Signature**

```ts
declare const DialectDatabase: DialectDatabase<T>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L9)

## DrizzleCapability

**Signature**

```ts
declare const DrizzleCapability: DrizzleCapability;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L10)

## DrizzleOverrides

**Signature**

```ts
declare const DrizzleOverrides: DrizzleOverrides<Tables>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L11)

## DrizzleServiceDescriptor

**Signature**

```ts
declare const DrizzleServiceDescriptor: DrizzleServiceDescriptor<Id, Client, Schema, Models>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L12)

## FindManyArgs

**Signature**

```ts
declare const FindManyArgs: FindManyArgs<T>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L13)

## FindOneArgs

**Signature**

```ts
declare const FindOneArgs: FindOneArgs<T>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L14)

## InsertArgs

**Signature**

```ts
declare const InsertArgs: InsertArgs<T>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L15)

## ModelDescriptor

**Signature**

```ts
declare const ModelDescriptor: ModelDescriptor<T, Extensions>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L16)

## ModelExtensionContext

**Signature**

```ts
declare const ModelExtensionContext: ModelExtensionContext<T>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L17)

## Row

**Signature**

```ts
declare const Row: {
  [
    K in keyof {
      [Key in keyof T["_"]["columns"] & string]: T["_"]["columns"][Key]["_"]["notNull"] extends true
        ? T["_"]["columns"][Key]["_"]["data"]
        : T["_"]["columns"][Key]["_"]["data"] | null;
    }
  ]: {
    [Key in keyof T["_"]["columns"] & string]: T["_"]["columns"][Key]["_"]["notNull"] extends true
      ? T["_"]["columns"][Key]["_"]["data"]
      : T["_"]["columns"][Key]["_"]["data"] | null;
  }[K];
};
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L18)

## TableOverrides

**Signature**

```ts
declare const TableOverrides: TableOverrides<T>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L19)

## TableZodSchemas

**Signature**

```ts
declare const TableZodSchemas: TableZodSchemas<T>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L20)

## Update

**Signature**

```ts
declare const Update: Partial<{
  [
    K in keyof ({
      [
        Key in keyof T["_"]["columns"] & string as RequiredKeyOnly<Key, T["_"]["columns"][Key]>
      ]: T["_"]["columns"][Key]["_"]["notNull"] extends true
        ? T["_"]["columns"][Key]["_"]["data"]
        : T["_"]["columns"][Key]["_"]["data"] | null;
    } & {
      [
        Key in keyof T["_"]["columns"] & string as OptionalKeyOnly<
          Key,
          T["_"]["columns"][Key],
          false
        >
      ]?:
        | (T["_"]["columns"][Key]["_"]["notNull"] extends true
            ? T["_"]["columns"][Key]["_"]["data"]
            : T["_"]["columns"][Key]["_"]["data"] | null)
        | undefined;
    })
  ]: ({
    [
      Key in keyof T["_"]["columns"] & string as RequiredKeyOnly<Key, T["_"]["columns"][Key]>
    ]: T["_"]["columns"][Key]["_"]["notNull"] extends true
      ? T["_"]["columns"][Key]["_"]["data"]
      : T["_"]["columns"][Key]["_"]["data"] | null;
  } & {
    [
      Key in keyof T["_"]["columns"] & string as OptionalKeyOnly<Key, T["_"]["columns"][Key], false>
    ]?:
      | (T["_"]["columns"][Key]["_"]["notNull"] extends true
          ? T["_"]["columns"][Key]["_"]["data"]
          : T["_"]["columns"][Key]["_"]["data"] | null)
      | undefined;
  })[K];
}>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L21)

## UpdateArgs

**Signature**

```ts
declare const UpdateArgs: UpdateArgs<T>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L22)

## UpsertArgs

**Signature**

```ts
declare const UpsertArgs: UpsertArgs<T>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L23)

## Where

**Signature**

```ts
declare const Where: Partial<{
  [
    K in keyof {
      [Key in keyof T["_"]["columns"] & string]: T["_"]["columns"][Key]["_"]["notNull"] extends true
        ? T["_"]["columns"][Key]["_"]["data"]
        : T["_"]["columns"][Key]["_"]["data"] | null;
    }
  ]: {
    [Key in keyof T["_"]["columns"] & string]: T["_"]["columns"][Key]["_"]["notNull"] extends true
      ? T["_"]["columns"][Key]["_"]["data"]
      : T["_"]["columns"][Key]["_"]["data"] | null;
  }[K];
}>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L24)

## activateDrizzleService

**Signature**

```ts
declare const activateDrizzleService: <
  Service extends DrizzleServiceDescriptor<any, any, any, any>,
>(
  service: Service,
  env: Readonly<Record<string, unknown>>,
) => Promise<DrizzleActivation<Service>>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L1)

## defineDrizzleService

**Signature**

```ts
declare const defineDrizzleService: <
  Client,
  const Schema extends Readonly<Record<string, unknown>>,
  const Models extends DrizzleModelMap<TablesOf<Schema>> = {},
>(
  options: DefineDrizzleServiceOptions<Client, Schema, Models>,
) => DrizzleServiceDescriptor<string, Client, Schema, Models>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L3)

## defineModel

**Signature**

```ts
declare const defineModel: <
  const T extends Table,
  const Extensions extends ModelExtensionMap<T>,
>(options: {
  readonly table: T;
  readonly extend: NonEmptyExtensions<Extensions>;
}) => ModelDescriptor<T, Extensions>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/index.ts#L2)

## `internal.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [DrizzleActivation](#drizzleactivation)
  - [DrizzleServiceDescriptor](#drizzleservicedescriptor)
  - [activateDrizzleService](#activatedrizzleservice)
  - [drizzleRuntimeOf](#drizzleruntimeof)

---

# utils

## DrizzleActivation

**Signature**

```ts
declare const DrizzleActivation: any;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/internal.ts#L1)

## DrizzleServiceDescriptor

**Signature**

```ts
declare const DrizzleServiceDescriptor: DrizzleServiceDescriptor<Id, Client, Schema, Models>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/internal.ts#L3)

## activateDrizzleService

**Signature**

```ts
declare const activateDrizzleService: <
  Service extends DrizzleServiceDescriptor<any, any, any, any>,
>(
  service: Service,
  env: Readonly<Record<string, unknown>>,
) => Promise<DrizzleActivation<Service>>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/internal.ts#L1)

## drizzleRuntimeOf

**Signature**

```ts
declare const drizzleRuntimeOf: (value: object) => DrizzleServiceRuntime;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/internal.ts#L2)

## `metadata.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [dialectFor](#dialectfor)
  - [extractTables](#extracttables)
  - [inspectTables](#inspecttables)

---

# utils

## dialectFor

**Signature**

```ts
declare const dialectFor: (table: Table) => "pg" | "mysql" | "sqlite";
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/metadata.ts#L32)

## extractTables

**Signature**

```ts
declare const extractTables: (schema: Readonly<Record<string, unknown>>) => TableMap;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/metadata.ts#L24)

## inspectTables

**Signature**

```ts
declare const inspectTables: (tables: TableMap) => {
  readonly dialect: "pg" | "mysql" | "sqlite";
  readonly metadata: Readonly<Record<string, TableMetadata>>;
};
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/metadata.ts#L8)

## `model.ts`

---

## Exports Grouped by Category

- [Database](#database)
  - [defineModel](#definemodel)
- [utils](#utils)
  - [RESERVED_OPERATIONS](#reserved_operations)
  - [createBoundModel](#createboundmodel)
  - [modelRuntimeOf](#modelruntimeof)

---

# Database

## defineModel

Adds named table methods with an injected table and transaction-aware Drizzle client.
Use service overrides, not extensions, to replace the six reserved CRUD operations.

**Example**

```ts
import { defineModel } from "@relkit/drizzle";
import { integer, sqliteTable } from "drizzle-orm/sqlite-core";

const users = sqliteTable("users", { id: integer().primaryKey() });
const model = defineModel({
  table: users,
  extend: { firstTen: ({ table, database }) => database.select().from(table).limit(10) },
});
console.assert(model.extensionNames.includes("firstTen"));
```

**Signature**

```ts
declare const defineModel: <
  const T extends Table,
  const Extensions extends ModelExtensionMap<T>,
>(options: {
  readonly table: T;
  readonly extend: NonEmptyExtensions<Extensions>;
}) => ModelDescriptor<T, Extensions>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/model.ts#L40)

Since v0.1.0

# utils

## RESERVED_OPERATIONS

**Signature**

```ts
declare const RESERVED_OPERATIONS: readonly [
  "findOne",
  "findMany",
  "insert",
  "update",
  "upsert",
  "delete",
];
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/model.ts#L12)

## createBoundModel

**Signature**

```ts
declare const createBoundModel: (binding: ModelBinding, model?: ModelDescriptorAny) => object;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/model.ts#L75)

## modelRuntimeOf

**Signature**

```ts
declare const modelRuntimeOf: (value: ModelDescriptorAny) => ModelRuntime;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/model.ts#L67)

## `operations.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [runOperation](#runoperation)

---

# utils

## runOperation

**Signature**

```ts
declare const runOperation: (
  binding: ModelBinding,
  name: string,
  args: unknown,
) => Promise<unknown>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/operations.ts#L3)

## `runtime-types.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [DRIZZLE_RUNTIME](#drizzle_runtime)
  - [DrizzleServiceRuntime (interface)](#drizzleserviceruntime-interface)
  - [MODEL_RUNTIME](#model_runtime)
  - [ModelBinding (interface)](#modelbinding-interface)
  - [ModelRuntime (interface)](#modelruntime-interface)
  - [TableMetadata (interface)](#tablemetadata-interface)

---

# utils

## DRIZZLE_RUNTIME

**Signature**

```ts
declare const DRIZZLE_RUNTIME: unique symbol;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/runtime-types.ts#L4)

## DrizzleServiceRuntime (interface)

**Signature**

```ts
export interface DrizzleServiceRuntime {
  readonly client: (context: { readonly env: Readonly<Record<string, unknown>> }) => unknown;
  readonly dispose?: (database: any) => unknown;
  readonly schema: Readonly<Record<string, unknown>>;
  readonly tables: TableMap;
  readonly models: DrizzleModelMap<TableMap>;
  readonly overrides: DrizzleOverrides<TableMap>;
  readonly metadata: Readonly<Record<string, TableMetadata>>;
  readonly zodSchemas: Readonly<Record<string, TableZodSchemas<Table>>>;
  readonly dialect: "pg" | "mysql" | "sqlite";
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/runtime-types.ts#L17)

## MODEL_RUNTIME

**Signature**

```ts
declare const MODEL_RUNTIME: unique symbol;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/runtime-types.ts#L5)

## ModelBinding (interface)

**Signature**

```ts
export interface ModelBinding {
  readonly drizzle: unknown;
  readonly table: Table;
  readonly dialect: "pg" | "mysql" | "sqlite";
  readonly inTransaction: boolean;
  readonly metadata: TableMetadata;
  readonly override: Readonly<Record<string, unknown>>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/runtime-types.ts#L29)

## ModelRuntime (interface)

**Signature**

```ts
export interface ModelRuntime {
  readonly table: Table;
  readonly extend: Readonly<Record<string, (context: any, ...args: any[]) => unknown>>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/runtime-types.ts#L12)

## TableMetadata (interface)

**Signature**

```ts
export interface TableMetadata {
  readonly columns: Readonly<Record<string, unknown>>;
  readonly selectors: readonly (readonly string[])[];
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/runtime-types.ts#L7)

## `service.ts`

---

## Exports Grouped by Category

- [Database](#database)
  - [defineDrizzleService](#definedrizzleservice)
- [utils](#utils)
  - [DefineDrizzleServiceOptions (interface)](#definedrizzleserviceoptions-interface)
  - [drizzleRuntimeOf](#drizzleruntimeof)

---

# Database

## defineDrizzleService

Defines the application's Drizzle service, table operations, and model customizations.
The client factory runs at activation, not during descriptor discovery.

**Example**

```ts
import { defineDrizzleService } from "@relkit/drizzle";
import { integer, sqliteTable } from "drizzle-orm/sqlite-core";

const users = sqliteTable("users", { id: integer().primaryKey() });
const service = defineDrizzleService({
  schema: { users },
  client: () => {
    throw new Error("Configure a Drizzle driver before activation");
  },
});
console.assert(service.capability.dialect === "sqlite");
```

**Signature**

```ts
declare const defineDrizzleService: <
  Client,
  const Schema extends Readonly<Record<string, unknown>>,
  const Models extends DrizzleModelMap<TablesOf<Schema>> = {},
>(
  options: DefineDrizzleServiceOptions<Client, Schema, Models>,
) => DrizzleServiceDescriptor<string, Client, Schema, Models>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/service.ts#L48)

Since v0.1.0

# utils

## DefineDrizzleServiceOptions (interface)

**Signature**

```ts
export interface DefineDrizzleServiceOptions<
  Client,
  Schema extends Readonly<Record<string, unknown>>,
  Models extends DrizzleModelMap<TablesOf<Schema>>,
> {
  readonly id?: string;
  readonly schema: Schema;
  readonly client: (context: { readonly env: ApplicationEnv }) => Client | Promise<Client>;
  readonly models?: Models;
  readonly overrides?: DrizzleOverrides<TablesOf<Schema>>;
  readonly dispose?: (database: Client) => unknown | Promise<unknown>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/service.ts#L16)

## drizzleRuntimeOf

**Signature**

```ts
declare const drizzleRuntimeOf: (value: object) => DrizzleServiceRuntime;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/service.ts#L128)

## `types.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [ApplicationEnv (type alias)](#applicationenv-type-alias)
  - [BaseOperations (interface)](#baseoperations-interface)
  - [DatabaseContext (type alias)](#databasecontext-type-alias)
  - [DeleteArgs (interface)](#deleteargs-interface)
  - [DialectDatabase (type alias)](#dialectdatabase-type-alias)
  - [DrizzleCapability (interface)](#drizzlecapability-interface)
  - [DrizzleModelMap (type alias)](#drizzlemodelmap-type-alias)
  - [DrizzleOverrides (type alias)](#drizzleoverrides-type-alias)
  - [DrizzleServiceDescriptor (type alias)](#drizzleservicedescriptor-type-alias)
  - [FindManyArgs (interface)](#findmanyargs-interface)
  - [FindOneArgs (interface)](#findoneargs-interface)
  - [Insert (type alias)](#insert-type-alias)
  - [InsertArgs (interface)](#insertargs-interface)
  - [ModelDescriptor (interface)](#modeldescriptor-interface)
  - [ModelDescriptorAny (type alias)](#modeldescriptorany-type-alias)
  - [ModelExtension (type alias)](#modelextension-type-alias)
  - [ModelExtensionContext (interface)](#modelextensioncontext-interface)
  - [ModelExtensionMap (type alias)](#modelextensionmap-type-alias)
  - [NonEmptyExtensions (type alias)](#nonemptyextensions-type-alias)
  - [OperationOverride (type alias)](#operationoverride-type-alias)
  - [Row (type alias)](#row-type-alias)
  - [TableMap (type alias)](#tablemap-type-alias)
  - [TableOverrides (type alias)](#tableoverrides-type-alias)
  - [TableZodSchemas (interface)](#tablezodschemas-interface)
  - [TablesOf (type alias)](#tablesof-type-alias)
  - [Update (type alias)](#update-type-alias)
  - [UpdateArgs (interface)](#updateargs-interface)
  - [UpsertArgs (interface)](#upsertargs-interface)
  - [Where (type alias)](#where-type-alias)

---

# utils

## ApplicationEnv (type alias)

**Signature**

```ts
type ApplicationEnv = keyof Relkit.ApplicationEnv extends never
  ? Readonly<Record<string, string>>
  : Readonly<Relkit.ApplicationEnv>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L131)

## BaseOperations (interface)

**Signature**

```ts
export interface BaseOperations<T extends Table> {
  findOne(args: FindOneArgs<T>): Promise<Row<T> | null>;
  findMany(args?: FindManyArgs<T>): Promise<Row<T>[]>;
  insert(args: InsertArgs<T>): Promise<Row<T>>;
  update(args: UpdateArgs<T>): Promise<Row<T> | null>;
  upsert(args: UpsertArgs<T>): Promise<Row<T>>;
  delete(args: DeleteArgs<T>): Promise<Row<T> | null>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L51)

## DatabaseContext (type alias)

**Signature**

```ts
type DatabaseContext<Service> = {
  readonly [Name in keyof TablesOf<Service[typeof SERVICE_TYPES]["schema"]>]: BaseOperations<
    TablesOf<Service[typeof SERVICE_TYPES]["schema"]>[Name]
  > &
    (Name extends keyof Service[typeof SERVICE_TYPES]["models"]
      ? ConsumerExtensions<Service[typeof SERVICE_TYPES]["models"][Name]>
      : {});
} & {
  readonly transaction: <Value>(
    run: (context: DatabaseContext<Service>) => MaybePromise<Value>,
  ) => Promise<Value>;
  readonly zodSchemas: {
    readonly [Name in keyof TablesOf<Service[typeof SERVICE_TYPES]["schema"]>]: TableZodSchemas<
      TablesOf<Service[typeof SERVICE_TYPES]["schema"]>[Name]
    >;
  };
};
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L170)

## DeleteArgs (interface)

**Signature**

```ts
export interface DeleteArgs<T extends Table> {
  readonly where: Where<T>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L47)

## DialectDatabase (type alias)

**Signature**

```ts
type DialectDatabase<T> = T extends SQLiteTable
  ? Omit<SQLiteAsyncDatabase<any, any, any>, "query">
  : T extends PgTable
    ? Omit<PgAsyncDatabase<any, any>, "query">
    : T extends MySqlTable
      ? Omit<MySqlAsyncDatabase<any, any>, "query">
      : never;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L74)

## DrizzleCapability (interface)

**Signature**

```ts
export interface DrizzleCapability {
  readonly kind: "drizzle";
  readonly dialect: "pg" | "mysql" | "sqlite";
  readonly tables: readonly {
    readonly name: string;
    readonly databaseName: string;
    readonly columns: readonly {
      readonly key: string;
      readonly name: string;
      readonly dataType: string;
      readonly notNull: boolean;
      readonly hasDefault: boolean;
      readonly primaryKey: boolean;
      readonly unique: boolean;
    }[];
    readonly selectors: readonly (readonly string[])[];
    readonly customMethods: readonly string[];
  }[];
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L135)

## DrizzleModelMap (type alias)

**Signature**

```ts
type Partial<{ readonly [Name in keyof Tables]: ModelDescriptor<Tables[Name], any>; }> = Partial<{
  readonly [Name in keyof Tables]: ModelDescriptor<Tables[Name], any>;
}>
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L104)

## DrizzleOverrides (type alias)

**Signature**

```ts
type DrizzleOverrides<Tables> = {
  readonly [Name in keyof Tables]?: TableOverrides<Tables[Name]>;
};
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L70)

## DrizzleServiceDescriptor (type alias)

**Signature**

```ts
type DrizzleServiceDescriptor<Id, Client, Schema, Models> = DescriptorBase<"service", Id> & {
  readonly capability: DrizzleCapability;
  readonly [SERVICE_TYPES]: {
    readonly client: Client;
    readonly schema: Schema;
    readonly models: Models;
  };
};
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L156)

## FindManyArgs (interface)

**Signature**

```ts
export interface FindManyArgs<T extends Table> {
  readonly where?: Where<T>;
  readonly orderBy?: {
    readonly field: Extract<keyof Row<T>, string>;
    readonly direction: "asc" | "desc";
  };
  readonly limit?: number;
  readonly offset?: number;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L26)

## FindOneArgs (interface)

**Signature**

```ts
export interface FindOneArgs<T extends Table> {
  readonly where: Where<T>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L23)

## Insert (type alias)

**Signature**

```ts
type { [K in keyof ({ [Key in keyof T["_"]["columns"] & string as RequiredKeyOnly<Key, T["_"]["columns"][Key]>]: T["_"]["columns"][Key]["_"]["notNull"] extends true ? T["_"]["columns"][Key]["_"]["data"] : T["_"]["columns"][Key]["_"]["data"] | null; } & { [Key in keyof T["_"]["columns"] & string as OptionalKeyOnly<Key, T["_"]["columns"][Key], false>]?: (T["_"]["columns"][Key]["_"]["notNull"] extends true ? T["_"]["columns"][Key]["_"]["data"] : T["_"]["columns"][Key]["_"]["data"] | null) | undefined; })]: ({ [Key in keyof T["_"]["columns"] & string as RequiredKeyOnly<Key, T["_"]["columns"][Key]>]: T["_"]["columns"][Key]["_"]["notNull"] extends true ? T["_"]["columns"][Key]["_"]["data"] : T["_"]["columns"][Key]["_"]["data"] | null; } & { [Key in keyof T["_"]["columns"] & string as OptionalKeyOnly<Key, T["_"]["columns"][Key], false>]?: (T["_"]["columns"][Key]["_"]["notNull"] extends true ? T["_"]["columns"][Key]["_"]["data"] : T["_"]["columns"][Key]["_"]["data"] | null) | undefined; })[K]; } = InferInsertModel<T>
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L19)

## InsertArgs (interface)

**Signature**

```ts
export interface InsertArgs<T extends Table> {
  readonly data: Insert<T>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L35)

## ModelDescriptor (interface)

**Signature**

```ts
export interface ModelDescriptor<
  T extends Table,
  Extensions extends ModelExtensionMap<T> = ModelExtensionMap<T>,
> {
  readonly table: T;
  readonly extensionNames: readonly Extract<keyof Extensions, string>[];
  readonly [MODEL_TYPES]: Extensions;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L95)

## ModelDescriptorAny (type alias)

**Signature**

```ts
type ModelDescriptorAny = ModelDescriptor<Table, ModelExtensionMap<Table>>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L103)

## ModelExtension (type alias)

**Signature**

```ts
type ModelExtension<T> = (context: ModelExtensionContext<T>, ...args: any[]) => unknown;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L86)

## ModelExtensionContext (interface)

**Signature**

```ts
export interface ModelExtensionContext<T extends Table> {
  readonly table: T;
  readonly database: DialectDatabase<T>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L82)

## ModelExtensionMap (type alias)

**Signature**

```ts
type Readonly<Record<string, ModelExtension<T>>> = Readonly<Record<string, ModelExtension<T>>>
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L90)

## NonEmptyExtensions (type alias)

**Signature**

```ts
type NonEmptyExtensions<Extensions> = keyof Extensions extends never ? never : Extensions;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L91)

## OperationOverride (type alias)

**Signature**

```ts
type OperationOverride<Args, Result> = (options: {
  readonly args: Args;
  readonly base: (args: Args) => Promise<Result>;
}) => MaybePromise<Result>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L60)

## Row (type alias)

**Signature**

```ts
type { [K in keyof { [Key in keyof T["_"]["columns"] & string]: T["_"]["columns"][Key]["_"]["notNull"] extends true ? T["_"]["columns"][Key]["_"]["data"] : T["_"]["columns"][Key]["_"]["data"] | null; }]: { [Key in keyof T["_"]["columns"] & string]: T["_"]["columns"][Key]["_"]["notNull"] extends true ? T["_"]["columns"][Key]["_"]["data"] : T["_"]["columns"][Key]["_"]["data"] | null; }[K]; } = InferSelectModel<T>
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L18)

## TableMap (type alias)

**Signature**

```ts
type TableMap = Readonly<Record<string, Table>>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L11)

## TableOverrides (type alias)

**Signature**

```ts
type TableOverrides<T> = {
  readonly [Name in keyof BaseOperations<T>]?: OperationOverride<
    Parameters<BaseOperations<T>[Name]>[0],
    Awaited<ReturnType<BaseOperations<T>[Name]>>
  >;
};
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L64)

## TableZodSchemas (interface)

**Signature**

```ts
export interface TableZodSchemas<T extends Table> {
  readonly select: ZodType<Row<T>>;
  readonly insert: ZodType<Insert<T>>;
  readonly update: ZodType<Update<T>>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L120)

## TablesOf (type alias)

**Signature**

```ts
type TablesOf<Schema> = {
  readonly [Name in keyof Schema as Schema[Name] extends Table ? Name : never]: Extract<
    Schema[Name],
    Table
  >;
};
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L12)

## Update (type alias)

**Signature**

```ts
type Partial<{ [K in keyof ({ [Key in keyof T["_"]["columns"] & string as RequiredKeyOnly<Key, T["_"]["columns"][Key]>]: T["_"]["columns"][Key]["_"]["notNull"] extends true ? T["_"]["columns"][Key]["_"]["data"] : T["_"]["columns"][Key]["_"]["data"] | null; } & { [Key in keyof T["_"]["columns"] & string as OptionalKeyOnly<Key, T["_"]["columns"][Key], false>]?: (T["_"]["columns"][Key]["_"]["notNull"] extends true ? T["_"]["columns"][Key]["_"]["data"] : T["_"]["columns"][Key]["_"]["data"] | null) | undefined; })]: ({ [Key in keyof T["_"]["columns"] & string as RequiredKeyOnly<Key, T["_"]["columns"][Key]>]: T["_"]["columns"][Key]["_"]["notNull"] extends true ? T["_"]["columns"][Key]["_"]["data"] : T["_"]["columns"][Key]["_"]["data"] | null; } & { [Key in keyof T["_"]["columns"] & string as OptionalKeyOnly<Key, T["_"]["columns"][Key], false>]?: (T["_"]["columns"][Key]["_"]["notNull"] extends true ? T["_"]["columns"][Key]["_"]["data"] : T["_"]["columns"][Key]["_"]["data"] | null) | undefined; })[K]; }> = Partial<Insert<T>>
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L20)

## UpdateArgs (interface)

**Signature**

```ts
export interface UpdateArgs<T extends Table> {
  readonly where: Where<T>;
  readonly data: Update<T>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L38)

## UpsertArgs (interface)

**Signature**

```ts
export interface UpsertArgs<T extends Table> {
  readonly where: Where<T>;
  readonly create: Insert<T>;
  readonly update: Update<T>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L42)

## Where (type alias)

**Signature**

```ts
type Partial<{ [K in keyof { [Key in keyof T["_"]["columns"] & string]: T["_"]["columns"][Key]["_"]["notNull"] extends true ? T["_"]["columns"][Key]["_"]["data"] : T["_"]["columns"][Key]["_"]["data"] | null; }]: { [Key in keyof T["_"]["columns"] & string]: T["_"]["columns"][Key]["_"]["notNull"] extends true ? T["_"]["columns"][Key]["_"]["data"] : T["_"]["columns"][Key]["_"]["data"] | null; }[K]; }> = Partial<Row<T>>
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/drizzle/src/types.ts#L21)
