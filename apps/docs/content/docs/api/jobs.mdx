---
title: Jobs API
description: Generated public TypeScript reference for @relkit/jobs.
---

{/* Generated by apps/docs/scripts/generate.ts. Do not edit. */}

## `client-utils.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [assertOptionalText](#assertoptionaltext)
  - [assertOptions](#assertoptions)
  - [normalizeResult](#normalizeresult)
  - [notify](#notify)
  - [parseInput](#parseinput)
  - [resolveCorrelation](#resolvecorrelation)
  - [resolveProvider](#resolveprovider)
  - [runAbortable](#runabortable)

---

# utils

## assertOptionalText

**Signature**

```ts
declare const assertOptionalText: (value: unknown, name: string) => void;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client-utils.ts#L124)

## assertOptions

**Signature**

```ts
declare const assertOptions: (
  value: unknown,
) => asserts value is { readonly correlationId?: string };
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client-utils.ts#L117)

## normalizeResult

**Signature**

```ts
declare const normalizeResult: (
  value: JobProviderResult | undefined,
  profile: string,
  correlationId: string | undefined,
) => JobEnqueueResult;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client-utils.ts#L44)

## notify

**Signature**

```ts
declare const notify: <T>(
  hook: ((value: T) => void) | undefined,
  value: T,
  enabled?: boolean,
) => void;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client-utils.ts#L104)

## parseInput

**Signature**

```ts
declare const parseInput: (
  schema: StandardSchemaV1 | undefined,
  input: unknown,
) => Promise<unknown>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client-utils.ts#L34)

## resolveCorrelation

**Signature**

```ts
declare const resolveCorrelation: (value: JobClientOptions["correlationId"]) => string | undefined;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client-utils.ts#L113)

## resolveProvider

**Signature**

```ts
declare const resolveProvider: (
  source: unknown,
  profile: string,
  resolveProfile: ((profile: string) => unknown) | undefined,
) => JobProvider;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client-utils.ts#L16)

## runAbortable

**Signature**

```ts
declare const runAbortable: <A>(
  signal: AbortSignal,
  deadlineMs: number | undefined,
  work: () => Promise<A>,
) => Promise<A>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client-utils.ts#L62)

## `client.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [JobClient (interface)](#jobclient-interface)
  - [JobClientOptions (interface)](#jobclientoptions-interface)
  - [JobDeclaredEdge (interface)](#jobdeclarededge-interface)
  - [JobDependencyError (class)](#jobdependencyerror-class)
    - [code (property)](#code-property)
  - [JobEnqueueOptions](#jobenqueueoptions)
  - [JobEnqueueResult](#jobenqueueresult)
  - [JobInputValidationError (class)](#jobinputvalidationerror-class)
    - [code (property)](#code-property-1)
  - [JobInvocationBridge (interface)](#jobinvocationbridge-interface)
  - [JobInvocationBridgeOptions (interface)](#jobinvocationbridgeoptions-interface)
  - [JobObservedEdge (interface)](#jobobservededge-interface)
  - [JobOperationCancelledError (class)](#joboperationcancellederror-class)
    - [code (property)](#code-property-2)
  - [JobOperationContext (interface)](#joboperationcontext-interface)
  - [JobOperationTimeoutError (class)](#joboperationtimeouterror-class)
    - [code (property)](#code-property-3)
  - [JobProfileError (class)](#jobprofileerror-class)
    - [code (property)](#code-property-4)
  - [JobProvider (interface)](#jobprovider-interface)
  - [JobProviderError (class)](#jobprovidererror-class)
    - [code (property)](#code-property-5)
  - [JobProviderResult (type alias)](#jobproviderresult-type-alias)
  - [JobState](#jobstate)
  - [JobStatus](#jobstatus)
  - [createJobClient](#createjobclient)

---

# utils

## JobClient (interface)

**Signature**

```ts
export interface JobClient<Input = unknown> {
  readonly enqueue: (input: Input, options?: JobEnqueueOptions) => Promise<JobEnqueueResult>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L82)

## JobClientOptions (interface)

**Signature**

```ts
export interface JobClientOptions {
  readonly ownerId: string;
  readonly jobId: string;
  readonly source: unknown;
  readonly inputSchema?: StandardSchemaV1;
  readonly profile?: string;
  readonly resolveProfile?: (profile: string) => unknown;
  readonly bridge?: JobInvocationBridge;
  readonly signal?: () => AbortSignal;
  readonly deadline?: () => number | undefined;
  readonly correlationId?: string | (() => string | undefined);
  readonly declared?: boolean;
  readonly onDeclaredEdge?: (edge: JobDeclaredEdge) => void;
  readonly onObservedEdge?: (edge: JobObservedEdge) => void;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L67)

## JobDeclaredEdge (interface)

**Signature**

```ts
export interface JobDeclaredEdge {
  readonly kind: "enqueues-job";
  readonly from: string;
  readonly to: string;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L57)

## JobDependencyError (class)

**Signature**

```ts
declare class JobDependencyError { constructor(readonly jobId: string) }
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L108)

### code (property)

**Signature**

```ts
readonly code: "RELKIT_JOB_DEPENDENCY_UNDECLARED"
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L109)

## JobEnqueueOptions

**Signature**

```ts
declare const JobEnqueueOptions: JobEnqueueOptions;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L16)

## JobEnqueueResult

**Signature**

```ts
declare const JobEnqueueResult: JobEnqueueResult;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L16)

## JobInputValidationError (class)

**Signature**

```ts
declare class JobInputValidationError { constructor(readonly issues: readonly StandardIssue[]) }
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L86)

### code (property)

**Signature**

```ts
readonly code: "RELKIT_JOB_INPUT_VALIDATION"
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L87)

## JobInvocationBridge (interface)

**Signature**

```ts
export interface JobInvocationBridge {
  readonly run: <A>(
    operation: () => MaybePromise<A>,
    options?: JobInvocationBridgeOptions,
  ) => Promise<A>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L51)

## JobInvocationBridgeOptions (interface)

**Signature**

```ts
export interface JobInvocationBridgeOptions {
  readonly name: string;
  readonly attributes: Readonly<Record<string, unknown>>;
  readonly signal: AbortSignal;
  readonly kind?: "producer";
  readonly input?: unknown;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L44)

## JobObservedEdge (interface)

**Signature**

```ts
export interface JobObservedEdge {
  readonly relationship: "enqueues-job";
  readonly from: string;
  readonly to: string;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L62)

## JobOperationCancelledError (class)

**Signature**

```ts
declare class JobOperationCancelledError {
  constructor();
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L116)

### code (property)

**Signature**

```ts
readonly code: "ABORT_ERR"
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L117)

## JobOperationContext (interface)

**Signature**

```ts
export interface JobOperationContext {
  readonly operation: "enqueue";
  readonly signal: AbortSignal;
  readonly profile: string;
  readonly deadlineMs?: number;
  readonly correlationId?: string;
  readonly propagation?: TracePropagation;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L17)

## JobOperationTimeoutError (class)

**Signature**

```ts
declare class JobOperationTimeoutError {
  constructor();
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L124)

### code (property)

**Signature**

```ts
readonly code: "ETIMEDOUT"
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L125)

## JobProfileError (class)

**Signature**

```ts
declare class JobProfileError { constructor(readonly profile: string) }
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L93)

### code (property)

**Signature**

```ts
readonly code: "RELKIT_JOB_PROFILE_UNKNOWN"
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L94)

## JobProvider (interface)

**Signature**

```ts
export interface JobProvider {
  readonly enqueue: (
    input: unknown,
    options: JobEnqueueOptions,
    context: JobOperationContext,
  ) => MaybePromise<JobProviderResult>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L25)

## JobProviderError (class)

**Signature**

```ts
declare class JobProviderError {
  constructor();
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L100)

### code (property)

**Signature**

```ts
readonly code: "RELKIT_JOB_PROVIDER_UNAVAILABLE"
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L101)

## JobProviderResult (type alias)

**Signature**

```ts
type JobProviderResult = Pick<JobEnqueueResult, "instanceId" | "accepted"> &
  Partial<
    Pick<
      JobEnqueueResult,
      | "status"
      | "profile"
      | "correlationId"
      | "duplicate"
      | "idempotencyKey"
      | "idempotencyExpiresAt"
    >
  >;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L32)

## JobState

**Signature**

```ts
declare const JobState: JobState;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L16)

## JobStatus

**Signature**

```ts
declare const JobStatus: JobStatus;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L16)

## createJobClient

**Signature**

```ts
declare const createJobClient: (options: JobClientOptions) => JobClient;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/client.ts#L132)

## `define-job.ts`

---

## Exports Grouped by Category

- [Jobs](#jobs)
  - [defineJob](#definejob)
- [utils](#utils)
  - [DefineJobOptions (interface)](#definejoboptions-interface)
  - [IdempotencyDefinition (interface)](#idempotencydefinition-interface)
  - [JobDescriptor (interface)](#jobdescriptor-interface)
  - [RetryJitter (type alias)](#retryjitter-type-alias)
  - [RetryPolicy (interface)](#retrypolicy-interface)
  - [ScheduleDefinition (interface)](#scheduledefinition-interface)
  - [ScheduleOverlap (type alias)](#scheduleoverlap-type-alias)

---

# Jobs

## defineJob

Defines a durable job trigger with validated input, retry, timeout, and idempotency policy.
A retryable declared error may add an `afterMs` minimum delay; the provider
combines it with this job policy and direct function calls do not auto-retry.

**Example**

```ts
import { defineFunction } from "@relkit/app/functions";
import { defineJob } from "@relkit/app/jobs";
import { z } from "@relkit/app/schema";

const target = defineFunction({
  id: "email",
  input: z.string(),
  output: z.void(),
  handler: async () => undefined,
});
const job = defineJob({
  id: "email",
  input: z.string(),
  target,
  retry: { maxAttempts: 3, initialDelayMs: 100, maxDelayMs: 1_000, multiplier: 2, jitter: "full" },
});
void job;
```

**Signature**

```ts
declare const defineJob: <
  const Id extends string,
  const InputSchema extends StandardSchemaV1,
  const Target extends FunctionRefAny,
>(
  options: DefineJobOptions<Id, InputSchema, Target>,
) => JobDescriptor<Id, InferInput<InputSchema>, InputSchema, Target>;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/define-job.ts#L94)

Since v0.1.0

# utils

## DefineJobOptions (interface)

**Signature**

```ts
export interface DefineJobOptions<
  Id extends string,
  InputSchema extends StandardSchemaV1,
  Target extends FunctionRefAny,
> extends DescriptorMetadata {
  readonly id: Id;
  readonly input: InputSchema;
  readonly target: Target;
  readonly profile?: string;
  readonly retry: RetryPolicy;
  readonly timeoutMs?: number;
  readonly concurrency?: number;
  readonly schedule?: readonly ScheduleDefinition<InferInput<InputSchema>>[];
  readonly idempotency?: IdempotencyDefinition<InferInput<InputSchema>>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/define-job.ts#L60)

## IdempotencyDefinition (interface)

**Signature**

```ts
export interface IdempotencyDefinition<Input = unknown> {
  readonly key: IdempotencyKey<Input>;
  readonly retentionMs: number;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/define-job.ts#L39)

## JobDescriptor (interface)

**Signature**

```ts
export interface JobDescriptor<
  Id extends string,
  Input,
  InputSchema extends StandardSchemaV1 = StandardSchemaV1,
  Target extends FunctionRefAny = FunctionRefAny,
> extends DescriptorBase<"job", Id> {
  readonly input: InputSchema;
  readonly target: Target;
  readonly profile?: string;
  readonly retry: RetryPolicy;
  readonly timeoutMs?: number;
  readonly concurrency?: number;
  readonly schedule?: readonly ScheduleDefinition<Input>[];
  readonly idempotency?: IdempotencyDefinition<Input>;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/define-job.ts#L44)

## RetryJitter (type alias)

**Signature**

```ts
type RetryJitter = "none" | "full" | "equal";
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/define-job.ts#L15)

## RetryPolicy (interface)

**Signature**

```ts
export interface RetryPolicy {
  readonly maxAttempts: number;
  readonly initialDelayMs: number;
  readonly maxDelayMs: number;
  readonly multiplier: number;
  readonly jitter: RetryJitter;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/define-job.ts#L17)

## ScheduleDefinition (interface)

**Signature**

```ts
export interface ScheduleDefinition<Input = JsonValue> {
  readonly id: string;
  readonly cron: string;
  readonly timezone: string;
  readonly input: Input;
  readonly overlap: ScheduleOverlap;
}
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/define-job.ts#L27)

## ScheduleOverlap (type alias)

**Signature**

```ts
type ScheduleOverlap = "skip" | "allow";
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/define-job.ts#L25)

## `index.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - ["./client.js" (namespace export)](#clientjs-namespace-export)
  - ["./define-job.js" (namespace export)](#define-jobjs-namespace-export)

---

# utils

## "./client.js" (namespace export)

Re-exports all named exports from the "./client.js" module.

**Signature**

```ts
export * from "./client.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/index.ts#L2)

## "./define-job.js" (namespace export)

Re-exports all named exports from the "./define-job.js" module.

**Signature**

```ts
export * from "./define-job.js";
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/index.ts#L1)

## `retry-policy.ts`

---

## Exports Grouped by Category

- [utils](#utils)
  - [validateRetry](#validateretry)

---

# utils

## validateRetry

**Signature**

```ts
declare const validateRetry: (value: RetryPolicy) => RetryPolicy;
```

[Source](https://github.com/rel-kit/relkit/blob/main/packages/jobs/src/retry-policy.ts#L3)
