{
  "$schema": "./node_modules/konsistent/konsistent.schema.json",
  "version": "v1",
  "conventions": [
    {
      "name": "package-shell-files",
      "description": "Every RelKit package owns one manifest, TypeScript project, and public entry file.",
      "paths": "packages/{packageName}",
      "must": {
        "haveType": "directory",
        "haveFiles": ["package.json", "tsconfig.json", "src/index.ts"]
      }
    },
    {
      "name": "package-entry-barrel",
      "description": "Package entry files stay pure barrels as package APIs grow.",
      "paths": "packages/{packageName}/src/index.ts",
      "must": {
        "areBarrelFiles": true,
        "importFromCurrentDir": false,
        "importFromParents": false
      }
    }
  ]
}
