{
  "name": "my-app",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.10",
  "scripts": {
    "dev": "relkit dev",
    "check": "relkit check",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "relkit build",
    "start": "relkit start",
    "jobs:trigger": "relkit jobs trigger --job exportOrders --input-file examples/export-orders.json --operation-id example-trigger"
  },
  "dependencies": {
    "@relkit/app": "0.5.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.10",
    "@relkit/cli": "0.5.0",
    "@relkit/testing": "0.5.0",
    "typescript": "5.9.3"
  }
}
